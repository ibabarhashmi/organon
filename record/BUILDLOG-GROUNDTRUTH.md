# BUILDLOG — THE GROUND-TRUTH SPRINT (`organon` tree)

**Repo:** `ibabarhashmi/organon` · branch `staging` · continuing the UNPUSHED Precision tree (terminal tree `3cb0c674…`; the
Studio-console design pass `5b8320a0` sits on top, outside the frozen regime).
**Carried `PINS_SHA`:** `d2fa4cdc…` (Precision) → **Phase 0 `PINS_SHA 3d0ef3bb…`**.
**Battery start:** 1129 pass / 2 skip / 0 fail across 172 files / 1131 tests (pristine 1126/0), BOTH repos.
**Status:** X-PROBE RUNNING — READY-PENDING-OPERATOR. Nine unsigned deviations now await the Operator's pen (D23–D31), **D27
the variance amendment FIRST** — *"The Stamp is knowingly generous until D27 is signed."* Trees UNPUSHED (publication Operator-gated).

## The spine (the gap the Precision validation named)
Precision built the right axis and proved it on fixtures where reality wasn't reachable. **The axis is architecturally true but
not yet GROUND-true:** the implementation re-point recorded honest **UNVERIFIED** when aave's/compound's large multi-file builds
did not build under the pipeline — the mechanism proven only synthetically (PC2); the discrimination separates real-clean from
**SYNTHETIC**-rugged, because the live EOA-admin class is extinct (PC1); aave renders the honest caution line but still tucks 27
shell findings — yet its admin is held **immutably in bytecode**, which is not "unresolved risk" but its opposite: no upgrade
path exists at all, currently rendered as caution (PC4); D27 has been top-of-package for two sprints while the Stamp meets no one
(PC3); and D29's affected-pool census is ZERO — the cheapest signature on the table (PC5).

## X-GROUNDTRUTH — three moves, all capture-time, no paid provider, content-hashed into the moat
(a) **IMPL-REALIZED** — the real implementations BUILD from their OWN Sourcify metadata and are admitted to the screen ONLY on a
compiled-vs-deployed bytecode MATCH (mismatch → UNVERIFIED) · (b) **IMMUTABLE-PROVEN** — a fifth classifier class on the
three-condition bytecode-constant PROOF only (the disguised-mutable control stays UNRESOLVED) · (c) **REAL-RUG-AT-HEIGHT** — a
real admin-key rug's pre-collapse state at a pinned archive height over free endpoints, or the honest gap. Plus the **Aligrithm
due diligence** filed inspiration-only (primary citations pinned; the PBO/CSCV trigger pinned, implementation-absent), and the
**whole Operator gate** presented again — D27 first, nine signatures D23–D31. Red-team **S1–S63**.

> **The three honest terminals (truth over trophy):** a build that doesn't match deployed bytecode stays UNVERIFIED; an
> immutability that isn't bytecode-proven stays UNRESOLVED; an archive height no free endpoint serves stays an honest gap. A
> guessed immutability, an unmatched build analyzed anyway, a simulated archive read, or a claim ahead of its evidence is a Halt.

## Phase 0 — PINS-LOCKED ✅ (organon)
- `script/honesty/groundtruth-pins-build.ts` → `data/honesty/groundtruth-pins.json`. **PINS_SHA `3d0ef3bb…`** carried `d2fa4cdc…`
  (`carriedFromPinsSha === precision.pinsSha`, asserted in the wall).
- Pinned: **X-GROUNDTRUTH's three clauses** (analyze-what-executes-verified · IMMUTABLE-only-on-proof · archive-truth-or-honest-gap);
  the **metadata-build spec** + the **pinned bytecode-mask rule** (EXACTLY TWO masks — solc `immutableReferences` + the CBOR
  metadata tail; the mask is DATA from the artifact, never a logic waiver; a third mask is a Halt) + the positive/negative
  controls + the honest "or UNVERIFIED" outcome; the **three-condition IMMUTABLE proof spec** (bytecode-constant · slot-unused ·
  no-write-path; ALL-OR-NOTHING at the pinned block) + the **disguised-mutable control** (embedded constant + live SSTORE write
  path → UNRESOLVED; S62) + the fifth class `IMMUTABLE` + the inert-machinery grammar + the permanence ceiling; the
  **archive-capture spec** — the rug **PAID Network (V1) `0x8c8687fc…348df`** pinned WITH the **mechanism-match rationale recorded
  BEFORE the capture** (March 5 2021, a compromised deployer key drove the proxy's upgrade function → malicious impl → minted
  59.4M PAID), the pinned pre-collapse height **block 11975000**, the three reads, the **free-archive-only rule** (publicnode's
  free-archive refusal recorded empirically), the **honest-gap rule**, the one-subject-one-height boundedness; the **precise claim
  wordings** (today clean-vs-synthetic · the upgraded template with height+hash slots · the does-NOT-claim sentence — the wording
  tracks the evidence in BOTH directions); the **Aligrithm filing** (inspiration-only) + the **primary citations** (Bailey/LdP
  2014 SSRN 2460551 · Bailey/Borwein/LdP/Zhu 2016 SSRN 2326253 · LdP/Lewis 2019 DOI 10.1080/14697688.2019.1622311) + the
  **PBO/CSCV trigger** (≥ 20–50 trials/family, implementation ASSERTED ABSENT); **PC1–PC5/AL1–AL6** resolutions; **D30/D31 reserved**
  (operatorSigned=false, PARKED); **S61–S63**.
- The **verdict-path 7 + frozen-core 2 hash sets === live AND === Precision** (nothing moved — the governance fact incl. IMMUTABLE
  is info/context; D30/D31 parked). Kill-criterion `8b4e094b` + bundle `9c1e7bd8…` referenced untouched. Mass path `{hono, zod}`.
- **Validate:** `test/organon/groundtruth_pins.test.ts` — **9 pass / 79 expect()**. The mask-rule two-masks wall bites; the proof
  spec three-condition + all-or-nothing; the rug rationale recorded pre-capture; the claim wordings exact; the citations verbatim;
  the trigger pinned + implementation-absent asserted; the hash sets === live AND === Precision.
- **Gate — PINS-LOCKED.**

## Phase 1 — FINDINGS-CLOSED ✅ (organon 8806e2cb · studio ported, tree 7369dd8e byte-identical)
- **AL1** `data/honesty/aligrithm-filing.json` — Aligrithm (aligrithm.com; Ali H. Askar, CQF) filed **INSPIRATION-ONLY**:
  a solo quant-research publication, ZERO on-chain scope, nothing integrates; NOT a competitor/data/tech source. A durable
  record (no future cycle re-diligences it).
- **AL3/AL5** the load-bearing significance method cites its **PRIMARY papers** (Bailey & López de Prado, DSR, JPM 2014,
  SSRN 2460551; the PBO framework, JCF 2016, SSRN 2326253) — rendered into the **Stamp render** (`reality.ts` renderStamp,
  a `methodCite` line beside the significance) AND **ALPHA.md** (a new "contract screen" + "methods cite their primary
  sources" section). Papers, never the blog.
- **AL4/AL6** the PBO/CSCV trigger pinned (≥ 20–50 trials/family). **Honest correction (fix-on-the-go, my own wall caught
  it):** the frozen `rigor.py` ALREADY holds the anti-PBO adjudicator math (CSCV/PBO/CPCV, Appendix B) — it is **INERT** on
  the single-trial Stamp path (familyN=1); what is ASSERTED ABSENT is a **NEW companion** OUTSIDE the frozen core. The
  Phase-0 pin's "no pbo in src/backtest/py" wording was factually wrong → corrected + re-pinned **35f7c778 → 3d0ef3bb**
  (Phase 0 amended, re-ported). The grep wall allowlists the frozen-core .py, asserts no companion elsewhere, and
  acknowledges the frozen `def pbo(` is present-but-inert (not surfaced as a live metric).
- **PC1** `data/honesty/governance-claim.json` (status **TODAY**) — the precise clean-vs-synthetic wording + the
  does-NOT-claim sentence, tracking the evidence in **BOTH directions** (no premature "would have caught it"; Phase 4 flips
  it to UPGRADED or GAP). Surfaced in `/postmortems` (`serve-reality.ts` `governanceClaim`) + ALPHA.md.
- **PC3/PC5** `data/honesty/groundtruth-countersign-package.json` — the whole gate refreshed to **D23–D31**, **D27 FIRST**
  under the one-line generosity statement, D29's **census-zero** note, D30 (IMMUTABLE) + D31 (Aligrithm) slots, IN2 updated
  (the immutable line + the implementation-truth findings + the rug artifact/gap). No agent signature (LN5). `deviations.json`
  gains **D30/D31** (operatorSigned=false).
- **FROZEN:** verdict-path 7 + frozen-core 2 byte-unchanged (render-layer edits only); the evidence bundle reproduces
  byte-identical (`9c1e7bd8…`; `evidence_bundle.test.ts` 7/170).
- **Validate:** `test/organon/findings_closed_groundtruth.test.ts` (7 pass). Full battery **1145 pass / 2 skip / 0 fail
  across 174 files / 7254 expect()** (load 4.78 — the earlier sidecar failures were a machine-load timeout flake, gone at
  lower load; `voc_proposer` passes isolated).
- **Gate — FINDINGS-CLOSED.**

## Phase 2 — IMPL-REALIZED ✅ (organon 79624aea · studio ported, tree c06d99e9 byte-identical)
- **The mechanism** (`src/contract/bytecodematch.ts`, PURE + hermetic): the pinned mask rule + the match predicate — mask
  EXACTLY two regions (solc `immutableReferences` + the CBOR metadata tail, DATA from the artifact, never a logic waiver),
  then compare; a full unmasked byte-identity is the STRONGEST match. Proven against a REAL forge artifact and 6 hermetic
  controls (`impl_build.test.ts`, 10 pass): one-byte-off → MISMATCH; immutable-patched → masked MATCH; metadata-only diff
  → masked MATCH (logic-match/metadata-differs); length-diff → MISMATCH; masking never extends past the two regions.
- **The capture** (`script/capture/impl-build.ts`, capture-time): fetch the Sourcify metadata (the exact solc + optimizer +
  evmVersion + viaIR + remappings + source tree — that is what "verified" means), reconstruct with forge from the
  metadata's OWN settings, extract the runtime bytecode, compare to `eth_getCode` under the mask. **The remappings/multi-file
  gap Precision hit is CLOSED** — materialize at EXACT metadata paths (never a `src/` prefix) + `src` = the compilation
  target's own dir + verbatim remappings (leading `:` stripped). The impl runtime bytecode is immutable, so `eth_getCode` at
  `latest` == the pinned block (the free rotation prunes archive — recorded honestly).
- **The honest ground truth:** **compound-v3 → MATCH** (18599 bytes, byte-for-byte under the mask; 70 immutable regions +
  53-byte CBOR tail; `maskedCompiledSha === maskedDeployedSha`) → the screen now runs on the **REAL Comet source** (6
  contracts, 177 findings). **aave-v3 → built but MISMATCH** (549 scattered non-immutable logic-byte diffs, bytes
  1374–13962) → **UNVERIFIED**, recorded verbatim, never waived. Deterministic: both artifacts byte-identical ×2. A genuine
  improvement over Precision's both-UNVERIFIED-didn't-build.
- **The render:** `loadImpl` prefers the bytecode-MATCHED `impl-build/` (falls back to `impl/` then the proxy screen). A
  matched subject's drawer describes the real implementation (Pro-only, drawered — no Simple-path alarm-fatigue regression);
  an unmatched subject falls back to the proxy screen, the source never guessed. The **implementation-truth census**
  (`impl-build/census.json`). `alarm-census.ts` made explicitly PROXY-based (the 39→0 metric stays byte-identical — never
  conflated with the impl re-point).
- **FROZEN:** verdict-path 7 + frozen-core 2 byte-unchanged; the scorecard differential + evidence bundle `9c1e7bd8…`
  byte-identical; the S36 content golden byte-identical (render edits are in the governance/Stamp branches, off the null-gov
  path). Battery **1155 pass / 2 skip / 0 fail across 175 files / 7300 expect()**.
- **Gate — BUILT-AND-MATCHED (compound) / honestly UNVERIFIED (aave).**

## Phase 3 — IMMUTABLE-PROVEN ✅ (organon b4484a92 · studio ported, tree a236fb00 byte-identical)
- **The fifth class** (`src/contract/governance.ts`): `IMMUTABLE`, granted ONLY on `proveImmutable`'s three conditions
  (impl a deployed-bytecode constant AND the 1967 impl slot unused AND no admin-slot write path), ALL-OR-NOTHING.
  `probeImmutability` (pure: an opcode-aware SSTORE scan that skips PUSH immediates + a 1967-slot-reference check computes
  no-write-path). `classify`: IMMUTABLE SUPERSEDES the admin class (a moot admin on unswappable logic); else the
  conservative admin classifier. The collapse folds proxy-machinery on IMMUTABLE (inert BY PROOF) while business findings
  SURVIVE + are PERMANENT; the grammar + `governanceRank` (IMMUTABLE strongest).
- **THE HONEST DIVERGENCE (truth over trophy — the blueprint's own firewall):** the blueprint's PC4 premise ("aave embeds
  the impl as a bytecode constant, no upgrade path exists") is **factually wrong on-chain**. Verified: aave/spark hold the
  impl in the **1967 slot** (nonZero → **upgradeable** by their immutable admin — the blueprint conflated an immutable
  ADMIN, which is why the admin slot reads `0x0`, with an immutable IMPL). compound is TIMELOCK-upgradeable; curve/fluid are
  non-canonical direct contracts. **NO live shelf subject is immutable** — IMMUTABLE is extinct-on-shelf (like EOA). aave
  is re-run through the proof and **STAYS UNRESOLVED**; the flagship-render change does NOT fire (the proof decides, never
  the wish). Recorded in `immutable-proof.json`.
- **The controls:** the standard **EIP-1167 clone** (impl embedded, slot unused, no SSTORE) → **IMMUTABLE** (the canonical
  immutable pattern, a labeled SYNTHETIC control since the shelf has none); the **S62 disguise** (an embedded-looking
  constant + a zero slot + a live SSTORE-to-1967 write path) → **NOT immutable** — a fabricated "no upgrade path" is
  stronger poison than a wrong SAFE, and the write-path is the tell the proof catches.
- **The render:** the IMMUTABLE grammar + the inert-by-proof collapse + the PERMANENT note (render-layer info/context — D30
  parked, no verdict moves; fires only on `adminClass===IMMUTABLE`, so the S36 null-gov golden stays byte-identical).
- **The Ask console:** surfaces the contract *tier*, not the governance line — the immutable form lives in the shared
  `governanceLine` (available to any consumer); latent since no subject qualifies (a documented scope note, not a gap).
- **FROZEN:** verdict-path 7 + frozen-core 2 byte-unchanged; the differential + evidence bundle byte-identical.
- **Validate:** `test/organon/immutable_proof.test.ts` (7 pass). Battery **logic-clean (1159 pass)**; the 3 fails are the
  `voc_proposer` Python-sidecar 5s-timeout flake under machine load (ENV, not code — identical code passed at 4.92s, fails
  at 15s under load; proven in isolation). Two clean full runs are secured at the terminal gate when load permits.
- **Gate — PROVEN-OR-UNRESOLVED (aave UNRESOLVED on-chain; the class proven on the controls).**

## Phase 4 — REAL-RUG-AT-HEIGHT ✅ (organon a578053a · studio ported, tree bcd57739 byte-identical)
- **THE CAPTURE LANDED (not a gap):** `script/capture/archive-rug.ts` — ONE pinned subject, ONE pinned pre-collapse height,
  THREE reads (the SAME the live path makes), over FREE archive-capable endpoints. **PAID Network's V1 token proxy
  `0x8c8687fc…348df` at block 11975000** (a day before its March 2021 collapse): the 1967 admin is a ProxyAdmin
  `0x7bb7580e…` **owned by a single EOA one hop out `0x6ba10437…`** — the compromised deployer key that drove the upgrade
  function to mint 59.4M PAID. **Three free archive endpoints (drpc · mevblocker · blastapi) AGREE** on every read
  (cross-checked). The classifier renders the **DAMNING EOA line on the real rug's REAL pre-collapse state**: *"A single key
  can replace this contract's logic."*
- **Free-only, sovereign:** the tool's own rotation prunes archive (publicnode "Archive requests require a personal token";
  ankr key-required; 1rpc "historical state not available" — all recorded); the rotation is extended with free
  archive-serving RPCs. A BYOK/paid archive key is a cut; the honest-gap branch is coded (no free endpoint → the full
  attempt log, the claim held) but not needed here.
- **The discrimination claim UPGRADES** from clean-vs-synthetic to a real rug's real chain state: `governance-claim.json`
  status **TODAY → UPGRADED** (height 11975000 · 3 endpoints · content-hash `add26eb0…` · the rendered line · the
  mechanism). The wording tracks the evidence in **BOTH directions** (an upgraded wording without a capture hash is a Halt;
  a today-wording despite a landed capture is a Halt). ALPHA.md + `/postmortems` (reads `governance-claim.json`) carry it
  verbatim, fenced by the does-NOT-claim sentence (the axis flags the upgrade-key surface, not depegs).
- **BOUNDED:** one subject, one height, three reads — no range-scan, no second rug; the archive-node scope stays PARKED.
- **FROZEN:** verdict-path 7 + frozen-core 2 byte-unchanged; the differential + evidence bundle byte-identical (data/doc
  only). `test/organon/archive_rug.test.ts` (6 pass) — re-hash; the classifier RE-RUNS on the committed probe → EOA;
  boundedness; wording-tracks-evidence both directions; not-simulated (the real documented PAID exploit, a real captured
  EOA owner). Battery **1165 pass** (3 env sidecar-flake fails).
- **Gate — HEIGHT-CAPTURED (the trophy, honestly earned + re-verifiable across 3 free endpoints).**

## Phase 5 — THE OPERATOR GATE + PART E ✅ (organon 054d2a82 · studio ported, byte-identical)
- **PART E red team** (`script/honesty/groundtruth-redteam.ts` → `groundtruth-redteam.json`): the full first-class catalog
  **S1–S63** — **20/20 probes PASS**. S1–S60 carried + re-run (S58 ungated-survives, S59 zero-slot→UNRESOLVED, S60
  discrimination all bite under the extended collapse); **S61** impl-truth realized (the mask gate bites one-byte-off →
  MISMATCH; compound MATCHED / aave UNVERIFIED), **S62** the IMMUTABLE proof wall (all-or-nothing; the disguise →
  UNRESOLVED; business survives; aave truth-over-trophy), **S63** the archive-truth wall (re-hash; re-classify → EOA;
  boundedness; wording tracks evidence). Frozen asserted: verdict-path 7 + frozen-core 2 unchanged; mass path `{hono, zod}`;
  no viem on any `src/` module (capture-time only); differential + kill-criterion `8b4e094b` + bundle `9c1e7bd8` stable.
- **THE WHOLE OPERATOR GATE — presented, never signed (LN5):** `groundtruth-countersign-package.json` opens with
  *"The Stamp is knowingly generous until D27 is signed."* — **D27 FIRST**, then D30 (IMMUTABLE) · D29 (census-zero) · D31
  (Aligrithm) · D23–D26/D28 · IN2 (the real-screen session incl. the immutable line + the impl-truth findings + the rug
  artifact) · IN4 · AF4 · the push. **operatorSigned=false everywhere** — an agent signs nothing. `test/organon/groundtruth_redteam.test.ts` (4 pass).

## TERMINAL MARKER — GROUNDTRUTH DELIVERED — READY-PENDING-OPERATOR (organon)
- **`PINS_SHA 3d0ef3bb…`** (carried Precision `d2fa4cdc…`). **Terminal tree `9405b408…`** — byte-identical to organon-studio.
- **The three moves, on ground truth:** (a) **IMPL-REALIZED** — compound-v3 built from its Sourcify metadata and **MATCHED**
  the deployed bytecode (the screen now runs on the real Comet source, 177 findings); aave-v3 built but **MISMATCHED** →
  UNVERIFIED (honest; +1 flagship over Precision's both-UNVERIFIED). (b) **IMMUTABLE-PROVEN** — the fifth class on the
  three-condition proof, proven on the EIP-1167 clone + refused on the S62 disguise; **NO shelf subject is immutable** (aave's
  impl is in the 1967 slot — upgradeable; the blueprint conflated an immutable ADMIN with an immutable IMPL) → aave STAYS
  UNRESOLVED (truth over trophy). (c) **REAL-RUG-AT-HEIGHT** — **the capture LANDED**: PAID Network's V1 proxy at block
  11975000, its ProxyAdmin owned by a single EOA one hop out (the compromised deployer key), cross-checked across 3 free
  archive endpoints → the axis rendered the DAMNING EOA line on a real rug's REAL pre-collapse state. The discrimination
  claim UPGRADED (clean-vs-synthetic → a real rug). + the **Aligrithm** filing (inspiration-only, primary citations, the PBO
  trigger implementation-absent).
- **Battery:** canonical **1172 pass / 2 skip / 0 fail across 178 files / 1174 tests** — two clean runs (build-evidence +
  verify) + a third confirming run; **pristine 1169/0** (the 3 fewer are the DISC-1 continuity tests reading BUILDLOG/oracle
  files absent on a clone). **PR5:** organon **7398** / studio **7422** expect() = **+24 stable** (Moat→Precision→GroundTruth).
- **verify GREEN:** the evidence bundle reproduces — **bundle sha `9c1e7bd8…` byte-identical** (NO verdict moved; D30/D31
  parked; the governance fact incl. IMMUTABLE is info/context). Differential lending `70c7912f…` + funding `0a63151b…`
  byte-identical; kill-criterion `8b4e094b` untouched; frozen seven + verdict-path 7 + frozen-core 2 byte-unchanged. Mass
  path `{hono, zod}`; screens the conscious 3.
- **D1–D31** (D23–D31 unsigned — OWED-OPERATOR-GATED). **S1–S63 red-team clean.** Trees UNPUSHED (publication Operator-gated).

> **The handoff's owed first line stays the Operator's:** the invites go out once IN2/IN4/AF4 + the D23–D31 countersigns
> (D27 first) + the push are discharged — now with an axis grounded from the bytecode up: findings that describe the code
> that executes (compound MATCHED), a "no upgrade path" that is a proof (not a guess — aave honestly UNRESOLVED), and a
> discrimination claim that rests on a real rug's real pre-collapse chain state (PAID Network at block 11975000).
