# ORGΛNON — THE FAMILY SPRINT (V39): the instrument pays for the pen it moved, and finally speaks a number

> **X-DERIVE(a):** the header, the gate, and the terminal marker below are **GENERATED by `Rollup`** over committed artifacts (`bun run script/honesty/rollup.ts`), never hand-typed. The agent wrote the **phase prose** only — and prose names producers, never restates their values (S126). Every producer agrees with every other (S107) **and no deviation holds two states (S150)**.

**NO NEW LAW — a fourth sprint running.** Seventeen stand. Every V38 execution-audit defect was an existing law under-applied: **X-RECKON** caught the unpaid SEARCH (J-1); **X-SHOWN** caught the unshown z and the prose tree-hash (J-3, J-5); **X-DERIVE/S107** caught the contradictory D51 — arithmetic checked, state unchecked (J-4); **X-HONEST** caught a fact that says UNJUDGEABLE and calls itself delivered (J-7). The constitution is complete; the work was application. *An instrument for one person keeps all seventeen — they were never about the audience.*

---

## D33 + D67 — READ THIS FIRST. THE FIRST GATE SECTION, TWO ITEMS ALONE. THE AGENT SIGNS NEITHER (LN5).

**(1) D33 — RECOMPUTED, with the price counted, and its autopsy on the same line as its signature.** V38 flipped D33 → `SIGNABLE` on a statistical test it had **redesigned after it failed**, then wrote *"no SEARCH was incurred."* Branch (b) of DD-33 fired and **D56 was owed** (J-1). This sprint paid it: the redesign's SEARCH is committed and **hash-chained in `record/`** (`test-redesign-search.json`, its immutable `d56SearchLedgerHash` in `record/chain.json`, the third application of S122's answer — after D53's Halt-lift and D67's amendment). And D33 was **recomputed with it counted**. F-1 was right that the count cannot *mechanically* flip a verdict read off the cross-check agreements — so the one honest form (RP-1): **D33's state now carries `testRedesigns: 1`, never resetting, rendered at the gate forever.** *A pen that opened on the second version of a test is not the same pen as one that opened on the first, and the Operator must see which he holds.* **The flip SURVIVES** in value — the theory leg rests on a null distribution over **independent seeds** (i.i.d. by construction; the seed-mean SE is immune to serial autocorrelation), so the price does not overturn it.

**And the autopsy the pen ordered finally meets the signature it withheld (J-2, S142).** `src/backtest/effectiven.ts` READS the frozen `effective_n.py` and `rigor.py` and **derives** (X-SHOWN, never asserts) the answer to DD-53/RP-2: `effective_n.py` measures **both** axes in distinct functions — the SERIAL family (`effective_n_serial`, `integrated_autocorr_time` = τ_int, `nw_tstat`; Lo 2002's axis) **and** the cross-sectional `effective_breadth`. The serial family IS the i.i.d. axis, so F-2's premise is refuted by the code. **But** the frozen `rigor.psr` computes `n = len(returns)` internally and hard-codes `√(n−1)` — it takes no `n` parameter, so the serial correction **cannot be wired into the frozen core** without editing it. **⇒ a HARNESS-COMPOSITION gap: the rider STANDS, quantified** — direction (the frozen PSR/DSR overstate confidence on autocorrelated input) and magnitude (z inflated ≈ √τ_int; measured live on the module's AR(1) demo, and on ORGΛNON's own captured funding panel τ_int ranged 27–165 → confidence overstated ≈ 5–13×). *A fix on the wrong axis retires the warning — worse than no fix (RP-2).* **The deciding evidence is SHOWN** (S141/J-3): the header carries the z (`0.608`), the pre-registered acceptance region (`|z_mean| < 2`), and the pre-registration timestamp. The pen no longer unlocks on a boolean.

**(2) D67 — the amended kill-criterion, ⟨N⟩ STILL EMPTY, awaiting the pen** — *and now, for the first time, the instrument can feed it: the false-fire count says a NUMBER, so `changedByCompile` has something to be changed by.*

**Neither is signed. `operatorSigned: false`. The agent presents the whole gate and never signs it (LN5).**

## GENERATED HEADER (the machine wrote these claims — X-DERIVE(a))
```
FAMILY PINS_SHA: 2c299b9e55fb5fb2b6f7e7af42f5c6c5a370c0e59ba0131c9cf86b2f6c5ba528
   (chain: 2c299b9e ← 153628a9 ← ab4900ee ← 257684c0 ← 8c80367a ← 07d27f81 ← 96469dbb ← d90df3c7 ← 98a44bd8 ← 2b1dd373 ← cc08a77b ← 6b285eba ← 3d0ef3bb)
BATTERY:        1793 / 2 / 0 · 274 files · 11664 expect() · two runs identical: y
                Δ +55 / −0 — AND prev 1738 + added 55 − removed 0 === 1793  RECONCILES: y          ← S107
CENSUS:         83 → 80 — AND 83 − (recovered 0 + reFounded 12 + reclassified 1) === 80 − 10 new-this-sprint OU walls  ← S107 (FAMILY: the new-OU term added)
DEVIATION STATE: one producer (State.deviations), all renders ⟨y⟩ · any deviation holding two states ⟨0⟩   ← S150 (J-4)
D56:            SEARCH appended ⟨record/chain.json d56⟩ · estimand changed ⟨y⟩ · price paid ⟨y⟩              ← S140
D33:            state SIGNABLE · testRedesigns 1 (NEVER RESETS) · redesignSearches [record/chain.json d56]           (RP-1)
                evidence: z 0.608 · acceptanceRegion |z_mean|<2 · preRegisteredAt 2026-07-14            ← S141 (J-3)
                i.i.d. rider [STANDS, HARNESS-COMPOSITION-GAP]: overstates by ≈√τ_int, ON THE SAME LINE  ← S142 (J-2)
                effective_n measures ⟨SERIAL + CROSS-SECTIONAL⟩; the SERIAL axis is relevant; rider ⟨STANDS⟩ (RP-2)
                flip survives the price ⟨y⟩ · signed: false (LN5)
FALSE-FIRE:     own ⟨UNJUDGEABLE, window growing — LEADS⟩ · retro ⟨5 fires / 771d tvl-drawdown 30%, RETROSPECTIVE, revisable⟩ (RP-3)
                peg-floor 0.995 ⟨0 fires / 459d — USDC held; a USEFUL number⟩ · renders a NUMBER at the door ⟨y⟩ · blanket UNJUDGEABLE ⟨0⟩  ← S145 (J-7)
EXIT SET:       ⟨7⟩ · oracle-staleness coverage ⟨3/1284 SHELF — a PROXY; bar = the Operator's positions = 0⟩ (RP-6)
                TRIGGER FIRED ⟨y, as a FACT, algebra-trigger.json⟩ · algebra shipped ⟨y⟩ · NOT frozen at seven
ALGEBRA:        lineage ids before===after ⟨y⟩ AND exit evaluations byte-identical ⟨y, incl. UNJUDGEABLE⟩  ← S147
FAMILY:         cardinality ⟨set-op over the shelf⟩ · selectionRank ⟨DERIVED⟩ · authors ⟨0⟩ · ranks ⟨0⟩ · suggests ⟨0⟩
                familyN ⟨1 — D63 OFF, seeded activation FAILS⟩ · counts in the moat ⟨y⟩              ← S148
                filter on the hashed surface: OPTIONAL · fixture lineage ids moved ⟨0⟩              ← RP-4
LINEAGE VIEW:   ordering ⟨CHRONOLOGICAL, pinned⟩ · search count rendered ⟨y⟩ · copy ⟨PINNED VERBATIM, no LLM⟩ (RP-5)
                screens ⟨3⟩
VERIFY:         exit 0 — evidence-bundle-reproduces (9c1e7bd8 byte-identical — NO VERDICT MOVED) · frozen-set-intact
LAWS: 17 · minted: 0 (four sprints) · deps: 2 · capability added: 0 (the four ships pay a price, show a number, reach a seventh kind, count a family — none touches a verdict) · reachableHumans: 1 (BY DESIGN)
D67: ⟨N⟩ STILL EMPTY — awaiting the pen   ·   D27: FIRST — the fourteenth sprint
```

---

## PART D — THE BUILD PHASES (what was built; the walls that hold it)

### Phase 0 — PINS-LOCKED (`family-pins.json`, PINS_SHA `2c299b9e`, carrying `153628a9`)
Every contract pinned before a byte of Phase code: NO NEW LAW; the D56 rule generalised as S140; D69 (D33 recomputed); D70 (the seventh kind + the trigger); D71 (the enumerator); D72 (the lineage view, a PATH not a screen); D73 (the RPC exposure, D42 dissolved); D74 (the Merkle layer DEAD, not parked); MR13/MR17/MR18; the ten Part-A′ attacks; the seven Part-F blocking re-pins (RP-1..RP-7); walls S140–S150; the shed order. **Positive control:** a mutated contract word moves the sha. Wall: `family_pins.test.ts` (13 tests).

### Phase 1 — THE PRICE, THE EVIDENCE, THE RECORD (S140–S144, S150) — *never sheds*
- **S140 / D56 (DD-54):** `test-redesign-search.json` committed + hash-chained in `record/`; `Search.forTestRedesign` derives a SEARCH when the estimand changes; a redesign with no chained hash is UNPRICED (a Halt). The price is automatic forever.
- **S142 / DD-53 (the autopsy meets the signature):** `src/backtest/effectiven.ts` derives the axis determination from the frozen code (`data/honesty/effective-n-determination.json`); the i.i.d. rider renders on the same line as `SIGNABLE`, direction + magnitude named.
- **S141 (J-3):** `Evidence.forStateFlip` emits `{z, acceptanceRegion, preRegisteredAt}`; a boolean flip is REFUSED.
- **S150 / MR18 (J-4):** ONE `State.deviations()` producer; the base gate reads it (D51 ANSWERED, the supersession pointer where the "product or instrument?" question stood); a contradiction is unrepresentable (`State.contradiction` catches a seeded `D51: OPEN`).
- **S143 (J-5):** `Marker.validate` runs on the terminal marker; prose in the treeHash slot FAILS.
- **S144 (J-6):** `Clone.staleAgainst` — a clone battery whose commit differs from the terminal commit is stale; absent is stale-by-default.
Walls: `family_phase1.test.ts` (11 tests).

### Phase 2 — THE INSTRUMENT SAYS A NUMBER (S145) — *never sheds. The sprint's reason for being.*
`src/strategy/series.ts` materializes the observable series from a committed, content-hashed fixture (`false-fire-series.json`: a REAL 771-day USDC/fluid-lending TVL chart + a REAL 459-day USDC/USD peg series, both RETROSPECTIVE). `FalseFire.countBoth` (already built) is fed. **A NUMBER renders at the door:** tvl-drawdown 30% → **5 fires / 771 days**; peg-floor 0.995 → **0 fires / 459 days** (USDC held — a useful number). **RP-3:** the own-capture number LEADS (UNJUDGEABLE today, the window growing every day the cadence runs), the retrospective renders beneath with its revisability, the window disparity stated. A COUNT — no σ, no distribution, no probability, no suggested threshold; passes the ONE GUARD. Subject-gated (tvl per-pool, peg per-asset) so it never renders the wrong subject's count. Walls: `family_phase2.test.ts` (8 tests).

### Phase 3 — oracle-staleness + utilization-ceiling, THE SIXTH AND SEVENTH KINDS (S146, D70, D73) — *sheds third*
`Exit.oracleStaleness(feed, now)` (a TEMPORAL observable — `now − feed.updatedAt`, from a REAL Chainlink `latestRoundData().updatedAt` captured via one eth_call, block 25533022, tier REAL★) and `Exit.utilization(pool)` (a DIMENSIONLESS ratio; UNJUDGEABLE without captured borrow/supply). Both added to the enum (5→7), both evaluate deterministically, neither carries a price. **Coverage emitted honestly: 3/1284 SHELF — a PROXY for an unmeasurable bar (positions held: 0, RP-6).** The exit set reaches SEVEN; the pre-armed trigger FIRES **as a fact** (`algebra-trigger.json`, content-hashed). Walls: `family_phase34.test.ts`.

### Phase 4 — THE COMBINATOR ALGEBRA (S147) — *sheds first*
`src/strategy/algebra.ts` — `Obs` + `when · count · changed` (Peyton Jones / Eber / Seward, *Composing Contracts*). The seven kinds reduce to THREE combinators. **NON-EXECUTABLE BY TYPE** — a Condition returns a boolean; there is no constructor that returns an order (X-ADVICE by the type system). **ADDITIVE, two-sided (S147):** every fixture lineage id unchanged, AND every fixture exit evaluation byte-identical on the decision (fired + judgeable), including the UNJUDGEABLE cases — proven over the 7-kind × facts matrix. It SHIPPED (the set is not frozen). Walls: `family_phase34.test.ts` (10 tests total for Phase 3+4).

### Phase 5 — THE FAMILY ENUMERATOR + SELECTION RANK (S148, D71) — *never sheds. The builder's first half.*
`src/strategy/family.ts` — `Family.enumerate(filter)` is a SET OPERATION over the user's own stated constraints (a real count over the captured shelf, `shelf-attributes.json`); `Family.selectionRank(pick, family)` is **DERIVED, never asked** ("your pick is the highest-APY member of your stated filter — rank 1 of 2"). It **authors nothing, ranks no candidates for the user to pick from, suggests nothing** (each a seeded negative). **D63 OFF:** `familyN` stays 1, the deflation meter is dark, a seeded `familyN > 1` FAILS to light it — but the counts land in the moat, so a reversal lights it over history. **RP-4:** the filter is a NEW OPTIONAL hashed field (`Manifest.Filter`), included in `Store.lineageId` only when present — a manifest without one is byte-identical (no fixture id moved), a manifest with one is a new lineage. Walls: `family_phase5.test.ts` (8 tests).

### Phase 6 — THE LINEAGE VIEW (S149, D72) — *sheds second*
`src/strategy/lineageview.ts` — your manifests in this family, CHRONOLOGICAL and mechanically pinned (a seeded score-ordering FAILS), each with its own independent ternary Stamp (no aggregate, no "best", no total order), with the **SEARCH COUNT rendered beside them** ("you have filed N variants… that is N searches") — the comparison permitted because it carries its own price tag. **RP-5:** the copy is PINNED VERBATIM in `family-pins.json` — no LLM phrasing on this surface. A PATH, not a screen (screens stay 3). Walls: `family_phase6.test.ts` (5 tests).

---

## GENERATED GATE (D33 + D67 first; the menu presented, never chosen — LN5)
```
firstLine: the instrument speaks · manifests (real) 0 · cycles unprompted (real) 0 · published false · reachableHumans 1 (BY DESIGN) · D51 ANSWERED = INSTRUMENT
firstSection:
  D33: SIGNABLE · testRedesigns 1 (never resets) · redesignSearches [record/chain.json d56] · z 0.608 · region |z_mean|<2 · preRegisteredAt 2026-07-14 · i.i.d. rider [STANDS, HARNESS-COMPOSITION-GAP] on the same line · operatorSigned: false (LN5)
  D67: the amended kill-criterion — ⟨N⟩ STILL EMPTY, awaiting the pen; the false-fire count now feeds changedByCompile
D51:  ANSWERED = INSTRUMENT (n=1 BY DESIGN) — supersedes the base gate's "product or instrument?" question (MR18/S150); the pen ALREADY chose; the agent records it and never signs (LN5)
deviationStates (ONE producer): D51 ANSWERED · D33 SIGNABLE · D63 OFF · D27 FIRST
D62-R: Option A (ratify or strike — the pen's) · D69–D74 present · D46/D50/D54/D55 present
D27:  FIRST — the fourteenth sprint (the Stamp is knowingly generous until D27 is signed)
IN2:  the ONLY validation left — and every technical excuse for not performing it is now gone (the instrument speaks a number)
IN4 ✓ / AF4 (BLOCKED, no key)
guardEfficacy: COMPUTED — if UNJUDGEABLE a sixth sprint, said louder
LAWS: 17 · minted: 0 (four sprints) · exit kinds: 7 · deps: 2 · screens: 3 · familyN: 1 · published: false · reachableHumans: 1 (BY DESIGN)
Presented whole. NEVER signed (LN5).
```

## TERMINAL MARKER — emitted by `Rollup.terminalMarker()` (Marker-valid; `treeHash` a real re-derivable hash — S143; `verifyOutput` derived, never "green" typed)
```
TERMINAL TREE:  ⟨filled post-commit — git rev-parse HEAD^{tree}⟩   COMMIT: ⟨git rev-parse HEAD⟩   pushed: false (DERIVED — a public-host remote, not any clone)
PINS_SHA:       2c299b9e55fb5fb2b6f7e7af42f5c6c5a370c0e59ba0131c9cf86b2f6c5ba528
BATTERY:        1793 / 2 / 0 · expect 11664
VERIFY:         exit 0 — evidence-bundle-reproduces · frozen-set-intact (bundle 9c1e7bd8 byte-identical — no verdict moved)
CLONE:          verify exit 0 · battery 1793/2/0 (THIS tree's — clonedCommit 76cfe9f8, from an empty dir) · SELF-CONTAINED  ← S144 (J-6)
```

---

### Definition of done
The pen was moved to `SIGNABLE` on a test that had failed and been redesigned — **and this sprint paid for it, recomputed D33 with the price counted, and its state now carries, un-resettably, the number of test redesigns it took to get there.** The autopsy meets the signature: the i.i.d. limitation renders on the same line as the verdict, direction and magnitude named — the rider STANDS, because the frozen `rigor.psr` derives its own `n` and cannot be fed the serial correction. **And the instrument speaks:** the false-fire count says *your kill-condition would have fired 5 times in 771 days of a real chart, and 0 times against USDC's real peg* — the own captures leading, the revisable series beneath, the window disparity stated, and never a better threshold. The exit set reaches seven, the algebra's trigger fires as a fact and the algebra ships. **And the builder begins** — an accountant with perfect memory: you state a filter, it counts the family; you pick, it derives your rank; you file variants, it renders them in filing order with the search count beside them. **The meter stays dark by the pen's word — and every count lands in the moat, so the day that word is reversed, the deflation lights over history already recorded, with zero rework.** *The ruling cost the meter. It never cost the memory.*
