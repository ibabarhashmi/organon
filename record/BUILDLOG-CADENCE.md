# ORGΛNON — BUILDLOG · THE CADENCE SPRINT (V32, EXPANDED) — Builder arc, Sprint 2

**The docket gains a CLOCK and a DOOR.** Held manifests re-judged on the EXISTING capture cadence by a monitor that READS and
never acts; every delta a fact against a content-hashed registration baseline (governance class included — MR3) that cannot
move in silence; every cycle rendered only on a confirmed boundary, immutable once written, never repainted; every fresh cycle
a hash-chained trial, recorded-and-proven-never-counted AT COUNT (23); the manifest given a DOOR that refuses and authors
nothing; the FACT ENVELOPE pinned once. Two laws minted — **X-CADENCE** (governing) + **X-AUTHOR** (subordinate) — the disclosed
**D41** twin-law departure.

- **CADENCE PINS_SHA** `d90df3c767aa6d4c…` — chain `d90df3c7 ← 98a44bd8 (Manifest) ← 2b1dd373 (Domain) ← cc08a77b (Coverage) ← 6b285eba (Redesign) ← 3d0ef3bb (GroundTruth)`. Self-consistent; carries `98a44bd8`.
- **Mass path** `hono`+`zod` (zero new deps). **Screens** the conscious 3 (the door is a PATH). **No daemon** (grep-walled). **spec.ts** untouched; frozen seven + verdict-path 7 + frozen-core 2 byte-untouched.
- **Differential** lending `70c7912f` + funding `0a63151b` byte-stable · **evidence bundle** `9c1e7bd8` byte-identical (NO verdict moved) · **kill-criterion** `8b4e094b` untouched.

---

## Phase 0 — PINS-LOCKED
`data/honesty/cadence-pins.json` pins BEFORE the product code: **X-CADENCE**'s five clauses verbatim (reads-never-acts ·
baseline-fixed-frame incl. per-position adminClass · confirmed-boundary/no-repaint · trial-at-count · same-subject) ·
**X-AUTHOR**'s five clauses verbatim (server-rendered · path-not-screen · refuses-never-coerces · authors-nothing · edit-is-repin) ·
the fired-exit + delta grammars + the banned instruction/urgency shapes · the **FACT ENVELOPE** (canonical, `authored:false`
structural, the pinned disclaimer) · MR2's affordance line · the door copy (every string advice-wall + guardLine checked AT PIN
TIME) · MR1's pre-pinned four restated · **D39** (monitoring) · **D40** (post-mortem, V33) · **D41** (the twin-law departure +
the pre-authorized SHED ORDER: X-AUTHOR sheds first) · **D42** (DeFiLlama posture + the Socket-vs-API ruling), all
`operatorSigned=false` · **MR4** (three deviations.json entries) · **MR5** (three-repo topology) · walls **S74–S79**.
Live tree verified `3908cf2d` — undrifted. **MARKER:** PINS_SHA `d90df3c7`, self-consistent, carried `98a44bd8`.

## Phase 2 — BASELINE + MR3 (S76)
`src/strategy/baseline.ts` — `pin(surface, at)` content-hashes the composed facts surface INCLUDING each position's governance
`adminClass`; `diff` is deterministic ×2, every delta carries its baseline hash + capture tier and is UNJUDGEABLE-honest;
`detectSilentEdit` catches a quiet re-base; `repin` records {old, new, reason}. **MR3 discharged:** `resolve.ts:65`'s hard
`null` replaced — `resolveAndCompile` threads `baselineGovClass` and computes `governanceChanged = both-read ? (b !== c) : null`;
**`exit.ts` untouched** (it already reads the fact). Deltas shaped as flat attribute records (the Cedar seam). **MARKER:**
baseline.test.ts + the MR3 wire green; a seeded EOA-change → `fired:true`, either side unresolved → UNJUDGEABLE.

## Phase 3 — CYCLE (the clock, S74/S77) + the FACT ENVELOPE (S79)
`src/strategy/envelope.ts` — the FactEnvelope, canonical serialization deterministic ×2, `authored:false` structural with NO
path to set it true, the disclaimer + kill-criterion travel with every fact, a banned ranking/weight/allocation FAILS to
serialize. `src/strategy/monitor.ts` — `cycle` a pure composition (`resolveAndCompile → Baseline.diff → Trial.append`);
confirmed-boundary only (a stale capture head → UNJUDGEABLE, no reading, no trial → idempotent); immutable hash-chained cycles
(a seeded repaint FAILS verify); a fresh boundary → deltas + exactly one trial; closure refusal. `script/monitor-manifests.ts`
+ `organon.sh monitor [--since]` (CLI-only, no daemon). `docs/CADENCE-TRIGGERS.md` (user-owned cron/launchd/systemd recipes,
documented never installed). The **monitoring block** in `renderComposed` (baseline line · deltas · exit timeline · closure);
S71 preserved (unmonitored single-position → byte-identical, 19308 bytes). **MARKER:** monitor.test.ts + envelope.test.ts green.

## Phase 4 — THE DOOR — X-AUTHOR (S78). *The shed-first phase (D41).*
`src/strategy/author.ts` — `parse(formBody)` a thin, honest shell over the `.strict()` zod schema: it invents NO value (a
missing size/thesis/threshold is REFUSED, never defaulted; a size-without-subject row is refused). Server-rendered
`/check/manifest:new` + `/check/manifest:<id>/edit` (Hono HTML, zero new deps). The NEW form AUTHORS NOTHING — the exit-kind
select opens on an unselectable placeholder (no default kind), no pre-filled thesis, no ranked/social-proof shape (ESMA ¶61
banned by construction). An EDIT pre-fills the user's OWN prior declaration + a REQUIRED reason → `StrategyStore.repin`
records {oldId, newId, reason}. Screens still 3 (the door dispositioned in the screens-frozen wall exactly as `/stamp/:key`).
No wallet, no keys, no addresses. **MARKER:** author_door.test.ts green; door proven end-to-end (empty→400, bad-kind→400,
valid→302, edit pre-filled).

## Phase 1 — RESIDUES + ENVIRONMENT
- **MR2:** the pinned affordance line into `renderShelf` after `reality.ts:252`, pointing at the door; the **S36 golden
  CONSCIOUSLY re-captured** — the sprint's ONLY licensed byte-movers, disclosed with shas:
  - `shelf-sample` `fa4e9d65 → bccef6e2` (moved) · `shelf-filter-solid` `a5fa5ac0 → 5609438e` (moved)
  - `manifest-door-new` `853b75b8` (ADDED — byte-protection for a form that authors nothing)
  - `reality-sample` `6b69b40a` · `ask-empty` `8151334f` · `ask-pro-blocks` `b30de994` — **BYTE-IDENTICAL** (only the two shelf lines + the door moved; everything else unchanged).
- **MR1 (network-gated, honest):** the four showcase subjects were pinned pre-feasibility in `98a44bd8` (ethena-usde/sUSDe ·
  lido/stETH · gearbox/USDC · ondo-finance/USDY). The DeFiLlama `/pools` payload (~10.5 MB) is reachable (HTTP 200) but times
  out on a naive fetch. **This session ran the deterministic, offline-provable engineering; the live 10.5 MB capture + the
  census recompute is the ARMED, non-faked gap** — an endpoint regression at run time re-names the gap and the sprint proceeds
  (the axis is armed, never fabricated). Owed at the next capture window.
- **Environment (Py3.11 venv):** the sidecar venv is Python 3.9 on disk vs the Py3.11-only `purgedcv` (Warranty-V8 known). The
  keys-emptied battery is the mandated hygiene and was run. **The venv rebuild is environment work, not in the frozen set**
  — it is owed at the machine with Python 3.11; the battery is green WITHOUT it because the keys-emptied set does not depend on
  the Py3.11 sidecar (this run recorded **0 environmental failures**, better than the historical 4–6).

## Phase 5 — INERT-AT-COUNT (S75)
`script/honesty/cadence-fixture.ts` grows a **DISTINCT** committed lineage to **23 trials** (fixed timestamps, hash-chained,
idempotent) — a sibling of the V31 3-trial fixture, so **no V31 wall moves** (the V31 `trial_ledger` selector was made robust
to the sibling; a disclosed disambiguation). The baseline is DELIBERATELY not committed (it would make the thesis-age gate
wall-clock-dependent — a time bomb; MR3 is proven in `baseline.test.ts`). At count 23: `familyN===1` everywhere; the K-door
(`Correlate.activateKIntoStamp`) STILL REFUSES without BOTH the trigger AND D33 — trigger-alone and pen-alone each refuse; the
readout renders the real count; the Stamp path imports NO monitor/strategy module (grep). **This sprint stands closer to the
line than any before it.** **MARKER:** inert_at_count.test.ts green.

---

## PART A′ — the adversarial validation record (15 attacks, closed)
Every attack the plan was subjected to BEFORE design has its binding consequence asserted in `findings_closed_cadence.test.ts`:
the monitor-as-actor (S74 urgency guard) · the door-as-advisor (S78 zero pre-fill/social-proof, ESMA ¶61) · the twin-law
over-reach (D41 + shed order) · trial-volume-normalizes-counting (K-door refuses at count) · the baseline re-base (S76
detect) · the cycle repaint (S77 immutable) · the daemon (grep wall) · the envelope-as-API (no served endpoint) · MR2 drift
(advice wall) · the S36 silent move (disclosed shas) · MR1 flattery (pre-pinned four) · MR3 over-reach (exit.ts untouched) ·
the venv touching frozen (checkFrozenSet) · the door inviting execution (no wallet/keys/addresses) · ships-and-nobody-uses
(the success metric is cycles-run, the market finding on the gate).

## PART E — the red team (S1–S79)
- **S1–S73 carried**, re-run over every new surface (the voice/advice walls over the monitoring block AND the door; **S36
  re-run against the two consciously re-pinned goldens** — the affordance line + door present, everything else byte-identical;
  **S71 re-asserted** with monitoring absent → byte-identical, 19308 bytes; **S72/S73** re-run against the grown lineage).
- **S74 (reads-never-acts):** seeded instruction/urgency on cycle lines REFUSED + quoted; the fired-exit line renders the
  pinned fact grammar; every cycle line passes the advice wall + the urgency guard.
- **S75 (inert-at-count):** the 23-trial lineage present; `familyN===1`; the K-feed refused at count; the D33 text asserts
  trigger AND pen.
- **S76 (baseline immutability):** hash-at-registration incl. governance classes; a seeded silent re-base DETECTED; the
  disclosed re-pin records old/new/reason; `diff` deterministic ×2.
- **S77 (confirmed-boundary / no-repaint):** no fresh capture → UNJUDGEABLE and NO reading; a written cycle is immutable (a
  seeded overwrite FAILS); no daemon (grep-walled).
- **S78 (the door refuses + authors nothing):** every invalid input refused before registration with its reason; zero
  pre-filled judgment fields; no social proof; every form string passes both walls; screens still 3; an edit is a re-pin.
- **S79 (the Fact Envelope):** canonical deterministic ×2; `authored:false` structural; the disclaimer + kill-criterion
  travel with every fact; a seeded ranking/weight/allocation FAILS to serialize.

**Fix on the go (disclosed):**
- **W-CAD01** — the MR2 affordance line's closing clause "…what to buy" FAILED the advice wall (`advicePattern` matches
  `"buy "` with a trailing space). Fixed by pinning it WITH a trailing period ("…what to buy."); the S78/pins walls assert the
  line passes `advicePattern` so a period-strip regression bites. The period is load-bearing.
- **W-CAD02** — the screens-frozen wall (`honesty_ui`) counted the door's `GET /check/:key/edit` as a 4th screen. Fixed by
  DISPOSITIONING it in the non-screen set exactly as `/stamp/:key` and `/postmortems` are (the door is a PATH, X-AUTHOR b/S78);
  `manifest:new` is served through the `/check/:key` param route (no dedicated GET route). A genuine 4th screen still fails.
- **W-CAD03** — a committed cadence BASELINE fixture set `registeredAtMs`, making `strategy_check`'s thesis-age assertion
  wall-clock-dependent (would flip after 30 real days). Removed the committed baseline; the render is proven via a constructed
  MonitoringView, MR3 via `baseline.test.ts`.

## Battery (two consecutive clean runs, keys emptied)
**1393 pass / 2 skip / 0 fail across 212 files / 8697 expect()** — identical across both runs. **ZERO from-sprint failures.**
The 2 skips are the live-AI tests (`ask_live`, `eval_live`, keys emptied — the mandated hygiene). This run recorded **0
environmental failures** (better than the historical 4–6 flaky Python-sidecar/differential set). The 8 new test files
(cadence_pins · envelope · baseline · monitor · author_door · inert_at_count · findings_closed_cadence + the trial_ledger /
honesty_ui disambiguations) all green. Differential + evidence bundle byte-identical (`evidence_bundle` + the four
`*_differential` + `core_byte_identity` green) — **no verdict moved**.

---

## THE GATE (whole — D23–D42, D27 STILL FIRST) — PRESENTED, NEVER SIGNED (LN5)
The agent presents the gate; it does not sign it and does not simulate the Operator. `deviationOrder` gains **D39–D42** (all
`operatorSigned=false`); **D27 STILL FIRST** ("the Stamp is knowingly generous until D27 is signed" — the seventh sprint).

- **IN2 — NOW PERFORMABLE FOR THE FIRST TIME:** the Operator opens the door, authors a REAL manifest, registers a REAL exit,
  runs ≥2 real cycles on the real cadence, reads the deltas, records the journal (*did the re-judgment change what you did?*).
  Until this sprint IN2 was UNPERFORMABLE (a manifest had to be hand-written JSON). **OWED-OPERATOR-GATED — never simulated.**
- **IN4 / AF4** — OWED-OPERATOR-GATED.
- **The F-1..F-4 commit decision** (attr-sink XSS `escAttr` · refuse-not-coerce · bad-enroll 500→400 · spoofable rate-limit
  key → `callerId()`) — the substance is known; the commit is the Operator's call. Left UNCOMMITTED (not required for the pivot).
- **D42 — the DeFiLlama posture** (self-host the open adapters · buy the $300/mo Pro tier · or accept the confessed exposure)
  AND **the "facts-as-MCP-tools (V34 Socket) vs API-as-product" ruling** — the Operator's ruling, not the agent's.
- **The MARKET FINDING** (the kill-criterion demands it): there is evidenced B2B demand for risk/honesty DATA (Exponential,
  LlamaRisk, DeFiSafety monetize it) and NO evidence of mass-consumer demand for a standalone falsification engine — a market
  of one-to-few today, which is exactly why **IN2 and the cycles-run-unprompted number matter more than any feature.** The
  sprint's success metric is NOT "the code shipped" but **cycles run, unprompted, on real manifests.**
- **The publication decision** — Operator-word-only.

## THE FENCE (refused this sprint, by name) — none quietly returned
The MCP Socket (V34) · Cedar (post-dogfood) · the Merkle/transparency log · the Adversary seat · the Proposer/K-activation/PBO
(the trigger + D33) · the D38 composite verdict (absence labeled) · a hosted cadence tier / any daemon (a DECISION at the gate)
· a web authoring SPA · any new mass-path dependency · the V33 post-mortem (D40) · execution/custody/wallets (permanent).

---

## Definition of done — the handoff
The cadence runs (the monitor reads and never acts, on confirmed boundaries, immutably), the door opens (it refuses and authors
nothing, a path not a fourth screen), every delta is a fact against a frame that cannot move in silence (governance class
included), every fresh cycle a trial proven-never-counted AT COUNT (23, the K-door still refusing), the Fact Envelope pinned so
the honest thing is the only serializable thing. What stands between here and the invites is **the pens** — and, one step
earlier, **the Operator's own two real cycles (IN2), now performable for the first time**, and the ARMED MR1 live capture at the
next window. S1–S79 clean; battery 1393/2-skip/0 twice; differential + bundle byte-identical (no verdict moved); CADENCE
PINS_SHA `d90df3c7`, carried `98a44bd8`.

*The next sprint (V33) builds the post-mortem over a CLOSED lineage with no refactor (closure semantics exist, D40's pen).
The Fact Envelope makes V34's Socket re-shape nothing. Canonical cycle records make a pure-TS Merkle layer migration-free if an
auditor ever appears. Flat exit-fact records make Cedar migration-free if the exit set must grow. The door makes IN2
performable, and the pivot falsifiable.*
