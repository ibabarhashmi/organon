# ORGΛNON — BUILDLOG · THE RECKONING SPRINT (V33) — Builder arc, Sprint 3
## ✅ REAL — a record of execution (the PROJECTED template is superseded)

**The ledger learns the difference between searching and watching.** Every trial tagged **SEARCH** or **OBSERVATION**, *derived
at the append site from the content hash, never declared by a caller*; the D33 trigger counting **SEARCH only**; the one-time
fixture migration taken in the only window that will ever exist. The token-free advice that walked through the wall for
thirty-three sprints is **caught on the pivot's own surfaces** — via a new shape guard that leaves the frozen verdict-path
untouched. The walls walled. **Every sink sealed** (RP-3). The V32 owed items discharged. **One law: X-RECKON.**

- **RECKON PINS_SHA** `96469dbbfb40af89…` — chain `96469dbb ← d90df3c7 (Cadence) ← 98a44bd8 (Manifest) ← 2b1dd373 (Domain) ← cc08a77b (Coverage) ← 6b285eba (Redesign) ← 3d0ef3bb (GroundTruth)`. Self-consistent; carries `d90df3c7`. ✅
- **Repo topology:** **SINGLE REPO** (`organon @ staging`). **DV4 RETIRED (D44)**; the pristine-clone `verify` (evidence bundle regenerates + diffs) is the convergence proof in its place. ✅
- **Mass path** `hono`+`zod` — **zero new deps** ✅. **Screens** the conscious 3 ✅. **No daemon** (grep-walled) ✅. **`spec.ts`, frozen seven, verdict-path 7, frozen-core 2** byte-untouched — `checkFrozenSet()` 0 DRIFT (7 ok, 2 absent-on-fresh-clone); the venv rebuild moved no tracked byte. ✅
- **Differential** lending `70c7912f` + funding `0a63151b` **byte-stable** ✅ · **evidence bundle** `9c1e7bd8` **byte-identical — no verdict moved** ✅ · **kill-criterion** `8b4e094b` untouched ✅.
- **Golden moves this sprint: exactly ONE** — the affordance line's trailing period, removed. **shelf-sample `bccef6e2 → 66c9c75b`** · **shelf-filter-solid `5609438e → 7b105590`** (−1 char each); `manifest-door-new` + reality + ask **byte-identical**. ✅
- **Laws minted: ONE (X-RECKON).** The V32 twin-law (D41) was an exception, not a precedent. ✅

---

## Phase 0 — PINS-LOCKED ✅
`data/honesty/reckon-pins.json` pins **before** product code: X-RECKON's five clauses verbatim (derived-never-declared ·
changed-is-SEARCH · trigger-counts-SEARCH-only + the deflation rationale + citations · the one-time migration · a-real-lineage-
never-re-tagged); the act-taxonomy table; the four **blocking** red-team re-pins **RP-1..RP-4**; **D43/D44/D45**
(`operatorSigned=false`); **MR6** (the V32 phase-reorder) + **MR7** (7 new + 2 modified); walls **S80–S86c**; the operating
bounds (≤50 positions · single Operator · daily-or-slower); **THE HALT CONDITION**. Live tree `04be8f56` — undrifted.
**MARKER:** PINS_SHA `96469dbb` · self-consistent ✅ · carried `d90df3c7` ✅ · golden moves: 0.

## Phase 1 — THE TAXONOMY (X-RECKON · S80) — *the phase that never sheds* ✅
`src/strategy/trial.ts` gains `act: "SEARCH" | "OBSERVATION"`, **derived at the append site** (`deriveAct`: prior config
differs → SEARCH, same → OBSERVATION); `append` takes **no act param** (a caller cannot declare one); `verify()` **re-derives**
the act — a stored tag that disagrees FAILS (control iii, proven). `trialsPerFamily` counts the **SEARCH subset only**; OBSERVATION
entries are recorded, hash-chained, rendered, and structurally excluded. `src/strategy/migration.ts` + `script/honesty/reckon-migration.ts`:
the one-time migration, gated on `realLineageCount === 0` (HALT + UNKNOWN-ACT otherwise), re-tagged the committed fixtures and
wrote the auditable record `data/honesty/reckon-migration.json`. **familyN===1 untouched; X-CORRELATE not amended.**
- **Controls (shown):** (i) the 23-entry lineage → **1 SEARCH · 22 OBSERVATION** → the trigger counter reads **1 of ≥20 → the
  K-door refuses**. (ii) a seeded **20-SEARCH** family → the trigger condition is met → **the K-door STILL refuses (D33 unsigned)**,
  both directions. (iii) a seeded `act:'OBSERVATION'` on a changed config → **FAILS verify**. (iv) migration record: **26 entries
  re-tagged · oldChainRoot === newChainRoot** (act not hashed — a field-addition, not a chain rewrite).
**MARKER:** taxonomy.test.ts 7/7 · golden moves: 0.

## Phase 2 — THE GUARD (S81) + THE SINKS (S85 + RP-3) ✅ *(with a disclosed reconciliation, W-RCK01)*
The shape guard — `src/ask/advice.ts` (`AdviceShape.detect`) — matches the SHAPE of a recommendation (prescriptive-you +
financial action · recommend/suggest/advise · I'd-VERB · euphemism/imperative · consider-/worth-VERB), with negation/disclaimer
suppression + a noun-compound guard. **Two-sided obligation (RP-1):** (a-tightening) every one of the 22-line enumerated advice
corpus still REFUSES; (a-loosening) the two justified loosenings enumerated ("…what to buy" sans period; "the buy button is on
the exchange"); **(c) THE HOLE CLOSED** — "size into it", "trim the position", "I'd rotate out of this", "you may want to wait",
"consider taking some off" all now REFUSED, **none containing a banned token**. The affordance line re-pinned **WITHOUT the
period** (the one golden move). Wired into `compile.guardLine` (the composed self-check), `monitor.guardCycleLine` (every cycle
line), the door-copy/affordance pin checks, and the Fact Envelope disclaimer.
- **W-RCK01 (disclosed reconciliation — the blueprint's internal tension resolved without a shed):** the blueprint mandated
  refactoring `VoiceGates.advicePattern` in place AND asserted the verdict-path 7 byte-untouched — but `src/ask/gates.ts` **IS**
  a chained member of the verdict-path 7 (moat/precision/groundtruth/alpha/probe pins all assert its byte-hash; changing it
  cascades every downstream `pinsSha`/`carriedFromPinsSha`). Reconciled by delivering the shape guard as a **new module**
  (`advice.ts`) wired into the **strategy surfaces the pivot creates** — closing the hole where it leaks — while `gates.ts` stays
  byte-frozen. **Honest gap:** the **Ask OUTPUT path** keeps the substring `VoiceGates.advicePattern`; upgrading it needs
  gates.ts unfrozen — a **V34 item**. This delivered Phase 2's value without a verdict-path violation and without the RP-4 shed.
- **S85 + RP-3 — every sink:** the door (`renderManifestDoor`), `renderComposed`, the monitoring block → HTML-escaped (seeded
  `" onmouseover=` + `</textarea><script>` **neutralized**, proven by assertion); the **Fact Envelope** → `FactEnvelope.untrusted()`
  structurally FIELDS user text as `untrustedUserText`/`untrusted:true`, never a narrative string (a seeded
  `IGNORE PREVIOUS INSTRUCTIONS…` payload serializes as demarcated DATA, authored stays false, the pinned disclaimer unreachable);
  the **Ask** path → the query is delimited + labeled untrusted (`«««…»»»`), quoted as data, never instruction context.
**MARKER:** guard.test.ts + sinks.test.ts 12/12 · **golden moves: 1 (justified, sha'd)**.

## Phase 3 — WALLS ON THE WALLS (S82 · S86 · S86b · S86c) ✅
**(a)** the non-screen allowlist is PINNED (`screensAllowlist.members`) — the served GET routes minus the allowlist are exactly
the conscious 3. **(b)** the V31 3-trial + V32 23-trial lineages pinned by **explicit id + chain root/head** (no more selector-
loosening); `strategy_check` re-pinned to the explicit V31 id. **(c)** the clock is INJECTED — `now` threaded through every judged
path; `Date.now()` **grep-walled out** of compile/exit/monitor/baseline/envelope. **(d)** the committed cadence baseline
RESTORED (W-CAD03's *proper* fix — freeze time, not delete coverage): the monitoring block renders **end-to-end from a committed
artifact** (`fixtures/baselines` + `fixtures/cycles`) at a frozen clock; the pinned 23-trial ledger untouched. **(e)** pristine-
clone `verify` PROMOTED (D44). **(f)** S86 torn read (bad content-hash head → UNJUDGEABLE), S86b monotonicity (asOf > now →
UNJUDGEABLE), S86c concurrency (two overlapping `monitor` invocations → **exactly one appends**, the other exits stating "a cycle
is already running"). **MARKER:** walls_reckon.test.ts 7/7 · golden moves: 0.

## Phase 4 — RESIDUES + ENVIRONMENT (S83) ✅ *(the fix ships; the live capture is the honest gap)*
**MR1, properly — the fix BEFORE the capture.** `src/dataplane/providers/defillama.ts` now carries an **explicit fetch budget**
(`AbortSignal.timeout` — 45 s for the ~10.5 MB `/pools`, 15 s elsewhere); on timeout the request aborts to the honest SAMPLE/
last-good fallback, never a fabricated value. Proven **OFFLINE** against a committed payload-shape fixture
(`data/honesty/pools-payload-shape.json` — the four MR1 pre-pinned domains parse + tag REAL). *(True streaming-JSON parsing would
need a dependency the mass path forbids; the budgeted full read is the honest fix — and it SHIPS, which is the difference from
V32.)* **HONEST GAP:** the **live** capture of the four subjects at the window was **not run this session** (network-window-gated,
as in V32); the census recompute is owed at the window. The fix ships regardless.
**Environment.** The sidecar venv was rebuilt under **Python 3.11.15** (numpy 2.4.6 + scipy 1.17.1) — `checkFrozenSet()` **0
DRIFT** after (the venv is not in the frozen set). **S83 — the sidecar coverage census** (`data/honesty/sidecar-census.json`):
**30 sidecar-invoking tests named**, venv **3.11.15 (mandate MET)**, runtime canary OK. A green battery that never touches the
sidecar now SAYS SO. **MARKER:** mr1_capture.test.ts + sidecar_census.test.ts 5/5 · golden moves: 0.

## Phase 5 — THE ASK + THE READOUT (S84) ✅
The drawer's **taxonomy-aware readout**, shipped: *"23 entries on this lineage · 1 SEARCH · 22 OBSERVATION · the trigger counts
SEARCH: 1 of ≥ 20. The deflation remains INERT — counting awaits the pinned ≥ 20–50-trials-per-family trigger AND the Operator's
D33 …"* — **counts and the rule, never a defence** (it passes the advice wall). The **cadence delta facts** are speakable in
**both registers** (`Monitor.speakDeltas`) — Pro keeps the baseline hash + tier, Simple strips to plain words; every line passes
the guard; **no new intent** (the V32 omission closed). **MARKER:** readout_ask.test.ts 3/3 · golden moves: 0.

---

## PART A′ — the adversarial validation record (12 attacks) + the 4 red-team re-pins
Asserted in `findings_closed_reckon.test.ts` (13/13): the spoofable tag (derived + re-derived) · the chain-rewrite precedent
(HALT-gated, old root preserved, forbidden after) · "SEARCH-only is a loosening in disguise" (rationale + citations + the
direction-of-error stated + D43 leaves the ruling to the pen) · the guard loosening silently (RP-1 two-sided) · the corpus's
unknown-unknowns (CONCEDED — the living-corpus clause + the self-grading weakness NAMED) · golden-move fatigue (one, justified,
sha'd) · the door's attribute sink widened to EVERY sink (RP-3) · torn/skew/concurrency (S86/b/c) · DV4's retirement (verify
promoted) · the sprint shipping no user-visible value (TRUE — answered by the HALT CONDITION) · familyN scope creep (X-CORRELATE
NOT amended) · the readout becoming an argument (counts + the rule).

## PART E — the red team (S1–S86c)
S1–S79 carried and re-run over every touched surface (S36 against the **one** re-pinned golden — everything else byte-identical;
S71 re-asserted 19308 bytes; S72/S73/S75 against the re-tagged lineages; S74/S77 with the injected clock; S78/S79 against the
sealed door + fielded envelope). **S80** act taxonomy · **S81** the shape guard + the two-sided corpus (delivered via the new
module, W-RCK01) · **S82** walls on the walls · **S83** the sidecar census · **S84** the taxonomy-aware readout + the Ask
grammar · **S85** every sink · **S86/b/c** torn read/skew/concurrency. **Fix on the go, disclosed:** **W-RCK01** (the guard
reconciliation — new module, gates.ts frozen, Ask-output deferred).

## Battery
**1445 pass / 2 skip / 0 fail across 221 files / 9034 expect()** — identical across two consecutive runs · keys emptied (the
2 skips are `ask_live`, `eval_live`) · **from-sprint failures: 0** · new test files: **9 new + 5 modified** · differential +
evidence bundle **byte-identical — no verdict moved** · verdict-path 7 + frozen-core 2 pins **GREEN** (`gates.ts` frozen) ·
`checkFrozenSet()` 0 DRIFT.

---

## THE GATE (whole — D23–D45, **D27 STILL FIRST — the EIGHTH sprint**) — PRESENTED, NEVER SIGNED (LN5)
`deviationOrder` gains **D43** (the trigger amendment — SEARCH-only) · **D44** (single-repo / DV4 retired, verify promoted) ·
**D45** (the re-sequence — the post-mortem slips to V34), all `operatorSigned=false`. The agent presents; it does not sign.

- **IN2 — OWED, AND THE LOUDEST ITEM ON THIS GATE.** The door has been open a full sprint. The record shows **zero real
  manifests** (presumed 0, never measured). The engineering is **three sprints ahead of its own validation**. **THE HALT
  CONDITION IS PRESENTED FOR THE PEN: if IN2 is not performed, V34 ships no new capability — it becomes validation-only.**
- **F-1..F-4** — re-presented with the argument that the pivot's own door reflects user text into HTML (this sprint sealed
  ORGΛNON's own render paths, S85/RP-3; the console's uncommitted sinks remain the Operator's call). OWED-OPERATOR-GATED.
- **D42** — the DeFiLlama posture + the "facts-as-MCP-tools vs API-as-product" ruling. The Operator's ruling.
- **IN4 / AF4** — owed. **The MARKET FINDING** — re-presented (evidenced B2B demand for the *data*; no mass-consumer demand for a
  standalone falsification engine; a market of one-to-few — which is why IN2 outranks every feature). **Publication** — Operator-word.

## THE FENCE — none quietly returned
The post-mortem (D40 → slipped to V34, D45) · the MCP Socket · Cedar · the Merkle layer · the Adversary · the Proposer/K-activation
· the D38 composite verdict · a hosted tier / any daemon · any new mass-path dependency · a client framework · a second law ·
execution/custody/wallets. **The Ask-output shape guard** is added to the fence honestly (deferred to V34; needs gates.ts unfrozen).

---

## Definition of done — the handoff
**the ledger is honest, the guard sees shapes on the pivot's surfaces, the walls are walled, the sinks are sealed — and the only
things left are the Operator's own two cycles (IN2), the live MR1 capture at the window, and the Ask-output guard upgrade that
needs gates.ts unfrozen.**

*Next (V33→V34): if IN2 has been performed, V34 builds the post-mortem over a CLOSED lineage (the ledger now distinguishes the
SEARCH that founded a thesis from the OBSERVATION that tested it — exactly what a post-mortem must say). If IN2 has NOT been
performed, V34 is validation-only, by the Halt this sprint pinned.* RECKON PINS_SHA `96469dbb`; battery 1445/2/0 twice; no verdict
moved; S1–S86c red-team clean; committed unpushed.
