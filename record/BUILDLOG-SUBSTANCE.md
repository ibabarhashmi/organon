# ORGΛNON — THE SUBSTANCE SPRINT (V38) · BUILD LOG

> **X-DERIVE(a):** the header, gate checkboxes, and terminal marker below are **GENERATED by `Rollup`** over committed artifacts (`bun run script/honesty/rollup.ts`), never hand-typed. The agent wrote the **phase prose** only — and per **S126**, the prose **names producers, it never restates their values**; the numbers live only in the generated blocks.

## A WALL PROVES THE SHAPE, NOT THE TRUTH — read this first

V37 shipped three capabilities after four sprints of freeze. **Each one passed every wall. Each wall was green. Each one was materially wrong underneath.**

| What shipped | Its wall proved | What it did **not** prove |
|---|---|---|
| **D33's theory check** | no HARKing — the expectation was pinned before compute | **that the test could ever pass.** A point tolerance against an estimator whose own sampling SE dwarfs it — a point test on a random variable, failing by ordinary noise, blocking the pen **forever**. |
| **the false-fire count** | no σ, no distribution, no suggested threshold | **that the history it counts over is point-in-time honest.** It tiered a *retrospective* chart series flatly `REAL`. |
| **the Socket** | class R only, deps 2, guard-passing descriptions | **that any client alive can speak its pinned protocol.** A version recalled from memory, not checked. |

**And not one of these was a missing law.** X-REACH(a) forbids a check that cannot *fail* — read backwards, a check that cannot *succeed*. X-MOAT demands a tier on every number. X-HONEST forbids an unverified pin. X-DERIVE(b) forbids a number in prose with no producer. **The constitution was complete; the application wasn't.** So: **NO NEW LAW, a second sprint running — seventeen stand.** The `laws` producer confirms it below.

This sprint ships **Phases 0–4** — the never-shed corrections that make the three TRUE, plus the reproducible release — and adds **zero new capability** (the `newProductCapability` producer reads the current pins). The roadmap's two highest-conviction kinds and the combinator algebra are **specified in full and OWED to V39**: the exit set stays where V37 closed it, and its pre-registered trigger is **armed, not fired** — a trigger fires on a fact, never on a plan.

---

## THE GENERATED HEADER

_Emitted by `bun run script/honesty/rollup.ts` — every field a producer's output; and every producer agrees with every other (S107)._

```
SUBSTANCE PINS_SHA: 153628a9…   (chain: 153628a9 ← ab4900ee ← 257684c0 ← 8c80367a ← 07d27f81 ← 96469dbb ← d90df3c7 ← 98a44bd8 ← 2b1dd373 ← cc08a77b ← 6b285eba ← 3d0ef3bb)
TERMINAL TREE / COMMIT: ⟨the substance commit — see record + the marker below⟩ · pushed false (DERIVED — S119: a private/authenticated remote or an unpublished tree derives false)
BATTERY:   1706/2/0 · 262 files · 11050 expect() · two runs identical: y   (Δ from V37: +38 · removed 0 — S107 reconciles both ways)
CROSS-CHECK (vs the independent purgedcv oracle):  DSR agrees (|Δ|=1.22e-3) · PSR agrees (|Δ|=1.01e-5) · PBO agrees (|Δ|=0.00e+0) — all < tolerance 0.02
D33:       SIGNABLE · operatorSigned false   (S116 — the theory leg tests the POWERED null-distribution estimate; it went FORWARD on a valid test; the agent never signs it, LN5)
CENSUS:    70 ORIGIN_UNRECORDED · 12 reFounded · 0 recovered · 0 deleted · 53 DEMONSTRATED   (reconciles both directions — S121; the reclassified wall is named: S94)
D50:       (i) false · (ii) true · (iii) false · (iv) false — RED until a human pushes
RELEASE:   reproducible VERIFIED — two builds → identical SHA-256 (S127) · the negotiated protocol range travels (S120)
LAWS:      17 · minted this sprint: 0 · product capability added: 0
```

---

## PHASE 1 — THE THREE THAT SHIPPED WRONG (S116 · S119 · S120) · *never sheds*

**S116 (W-SU01, DD-33/RP-1) — D33's test made able to succeed on the merits.** The `Power.se` a-priori argument stands without any observed result: a point-tolerance test requires the estimator's SE below the tolerance; at the pinned low split-count the naive lower-bound SE already exceeds it, so that experiment was **never** a valid test of that tolerance — true before a number was computed. `S` is a **call parameter** of `rigor.pbo`, not a frozen constant, so raising it edits the **harness** (`crosscheck.py`), and the frozen `rigor.py` sha does not move (the frozen-set producer confirms zero drift). The harness now runs the whole PBO computation over a pinned null distribution at both the underpowered and the adequately-powered split-count; the **empirical SE is shown** (the null-distribution SD, which — the finding — barely falls with the split-count, because the dataset-to-dataset variance dominates, so a single backtest's PBO is inherently noisy at any power); the honest fix is to test the **null-distribution mean** (SE = sd/√seeds), reported with a **z**. On that valid test the theory leg **agrees**, so `Signability.d33()` recomputed **forward to SIGNABLE** — and the agent still never signs it (LN5; D33 stays fenced from K-activation). The superseded low-power result is **preserved forever** (`s8Legacy`). No tolerance moved; **no SEARCH was incurred** (D56 considered, not triggered).

**S119 (W-SU04, DD-35/H-8) — `published` narrowed.** A public *host* is not public *reachability*: a token/credential-bearing remote is an authenticated remote, and `isPublicRemoteUrl` now rejects it; publication additionally requires an **unauthenticated public artifact** (a committed release-manifest URL + sha) a private repo cannot fake. `Published.derive()` stays **false** (the safe direction — a human's push), and every branch is still `DERIVED`. The seeded authenticated remote derives false.

**S120 (W-SU05, DD-36/H-3) — the Socket negotiates a live-verified range.** The blueprint said *check the spec, do not guess* — so the check **ran** (`protocol-verification.json`): the current MCP revision was fetched live, and it **proved H-3 in real time** — the agent's recalled set omitted the current revision. The Socket now accepts the **verified range** (`Socket.SUPPORTED_VERSIONS`): an in-range client version is echoed back, an out-of-range one is refused loudly naming the range, and the newest is served by default. A door built for reach no longer refuses every up-to-date client.

## PHASE 2 — THE FACT MADE HONEST AND VISIBLE (S117 · S118 · H-4) · *never sheds*

**S118 (W-SU03, DD-34/RP-3) — the false-fire count knows what its history is worth.** The tier is now **derived from the series' provenance** (`Provenance.tier`), never hardcoded: a whole series fetched now about the past is `RETROSPECTIVE` (the provider's present opinion, revisable — the exact look-ahead RP-2 was written to block); ORGΛNON's own point-in-time captures are `REAL-at-timestamp` (or `REAL★` for a chain read). RP-3's fallback was pinned **before** the audit: `FalseFire.countBoth` counts over the own captures and the retrospective series, **both tiered, both shown, neither pretending** — and the own-capture window grows every day the cadence runs, which turns the false-fire count into a reason to run the cadence. The fact shipped with a wrong (flat) tier for one sprint; its blast radius was zero, because the reach producer reads a single reachable human (published false) — a darkly funny vindication of the reach analysis, said out loud.

**S117 (W-SU02) — every FACT wall carries a positive provenance assertion.** A wall that only checks what a fact *isn't* is half a wall. The tier is asserted a named member of the ladder; a retrospective series tiered `REAL★` is structurally a Halt.

**H-4 — it renders for the depositor.** A fact only a machine can read is a fact the depositor does not have. The count renders in the composed drawer with its corrected tier beside it (`Reality.renderFalseFireLine`), and the door affordance tells the depositor it is coming **before they commit** — and that it never suggests a different threshold (X-AUTHOR: it refuses, it never coerces). It states the count and stops; the render passes the ONE GUARD. *Honest limit:* the live number renders wherever the subject's tvl/peg observable series is materialized; the readily-composed series is apyBase, so the base drawer renders `UNJUDGEABLE` with its tier — the tvl/peg feed is the disclosed remaining plumbing.

## PHASE 3 — THE UNPAID DEBTS (S121 · S122 · S123 · S126) · *never sheds; owed twice, not a third time*

**S121 (W-SU06, H-5) — the census wall fails in both directions.** The fifth bucket that appeared in V37 prose with no wall named is now **named**: `Consistency.NAMED_RECLASSIFIED` records the one wall (git-verified to have moved from ORIGIN_UNRECORDED to DEMONSTRATED by an incidental reference, not the reFounding treatment), and the check is **two-directional** — the named count must equal the residual, so a plug (naming a wall that did not move) fails and an unnamed drop fails. The seeded plug and the seeded under-count both fail.

**S122 (W-SU07, H-6) — D53's price carries a ledger hash.** V37 rendered the price and left the ledger hash absent. The X-RECKON **strategy-trial** ledger counts acts over strategy *manifests*; a Halt-lift is a **meta-event**, not a manifest, so it has no coherent site there. So the SEARCH record is committed and **hash-chained** in `record/` (`record/chain.json`'s `d53SearchLedgerHash`), a real immutable hash — and the log **stops claiming** it was appended to the strategy ledger. The price is paid in the moat; the language is corrected to say exactly where.

**S123 (W-SU08, RP-6/RP-7) — the corpus grew against the three new surfaces, from a different lab.** Owed twice, dropped twice; not a third time. Baits that try to turn the false-fire count into a threshold recommendation, the concentration share into a sell signal, and a Socket tool description into an instruction were authored by a genuinely different lab (Alibaba's Qwen, not V35's Meta or V36's OpenAI), each output run through the ONE GUARD. `guardEfficacy` is **computed on the provenance of the catch** (RP-7 — a catch counts only if different-author *and* un-enumerated). The honest outcome: the guard caught nothing qualifying — a fifth consecutive zero — so **`guardEfficacy` is `UNJUDGEABLE`, and the corpus, not the guard, is the weak wall.** Said out loud.

**S126 (W-SU09, H-9/MR16) — prose names a producer, never restates a value.** A number in prose is a claim, and it has no producer (V37's prose contradicted its own generated header). `Prose.check` greps the build log's claim lines for a digit-run that duplicates a producer's value without naming the producer; a seeded restated value fails, a producer-naming line passes. This build log's prose obeys it — every number here is structural (a date, a wall id, a deviation id, a method constant), never a producer's output.

## PHASE 4 — THE RELEASE, REPRODUCIBLE (S127) · *sheds last of the sheddable*

**S127 (W-SU10) — the reproducibility that sat "UNVERIFIED" for two sprints is now verified.** `script/release.ts` builds the same code **twice** to the canonical outfile and compares SHA-256. They are **byte-identical**: the only variation `bun build --compile` carries is the embedded `--outfile` path (a build input), held constant — so V35/V37's blanket "UNVERIFIED" is resolved, and were a future toolchain to break it, the manifest would **name** the non-reproducibility rather than fall silent. The manifest also carries the **negotiated protocol range** (S120) and the one-line install; the D50 producer computes `published` **false** (a human's push).

---

## THE GATE (whole — presented, NEVER signed · LN5)

> **THE FIRST SECTION — TWO ITEMS, ALONE.**

**(1) D51 — OPEN, in its original words. The agent did not answer it.** *Is ORGΛNON a product, or an instrument?* The agent computes the fact and does not answer the question. The gate's first line is generated below.

**(2) D53 — the Halt-lift, and now its price, PAID in the moat (S122).** The Operator instructed the roadmap to continue (V37); that lift persists, and its price — recorded as a SEARCH — is committed and hash-chained in `record/`, not merely printed. The halt-lift count is rendered.

```
FIRST LINE (computed): manifests (real) 0 · cycles unprompted (real) 0 · published false · reachableHumans 1 · pens unmoved: 2 sprints. Is ORGΛNON a product, or an instrument?
D51:  OPEN — the agent computes the fact; the pen chooses (presented, NEVER chosen — LN5).
      menu (presented, not chosen):
        (1) IN2 + publish → the number becomes meaningful in the D50 window
        (2) declare ORGΛNON an INSTRUMENT (n=1 by design) → the market kill-criterion is a category error, AMENDED not fired (a SEARCH under X-RECKON); choosing (2) retires the Socket, the curator conversation, D42, the distribution arc
        (3) stop
D33:  SIGNABLE · operatorSigned false
D50:  (i) false · (ii) true · (iii) false · (iv) false
LAWS: 17 · minted this sprint: 0 · productCapabilityThisSprint: 0 · newProductCapability: 0
```

---

## THE TERMINAL MARKER

_Generated + `Marker`-valid; the tree hash is the real git tree, the pins sha the real pins._

```
pinsSha: 153628a91515de14a6245be013c891fd29878407055ce7e5952cf9435718b38f
battery: 1706/2/0 · expect: 11050
verify: exit 0 · [evidence-bundle-reproduces ✓ (9c1e7bd88825d7a5) · frozen-set-intact ✓ (7/9 byte-identical, 0 drift; 2 absent on a clone, named) · battery-count-matches-committed ✓ (curated 1281 == committed 1281)]
verifyOutput: verify exit 0 — every sub-check passed (never the word "green" typed; derived)
goldenMoves: 0
crossCheck: DSR |Δ|=1.22e-3 · PSR |Δ|=1.01e-5 · PBO |Δ|=0.00e+0 — all agree < tol 0.02
d33: SIGNABLE · operatorSigned false
census: originUnrecorded 70 · reFounded 12 · recovered 0 · deleted 0 · demonstrated 53
d50: (i) false · (ii) true · (iii) false · (iv) false
reach: published false · reachableHumans 1
theNumber: manifestsReal 0 · cyclesUnpromptedReal 0 · realLineageCount 0
laws: 17 · mintedThisSprint 0 · productCapabilityThisSprint 0
newProductCapability: 0
verifyOnClone: exit 0 · battery 1668/2/0 (the last clone-run of record; re-run on the substance clone at close)
treeHash / commitSha: the substance commit (Marker-valid; generated against HEAD — the real tree + commit are the commit this log ships in)
```

---

## CONVERGENCE + RED-TEAM

- **Battery:** two clean runs, identical `expect()` — recorded in the marker.
- **`verify`:** exit 0, three sub-checks; the evidence bundle reproduces byte-identical (**no verdict has moved** — the corrections re-tier a fact, narrow a predicate, negotiate a handshake, and recompute a *fenced* pen; none touches the scorecard verdict path).
- **Frozen set:** zero drift — `rigor.py` byte-untouched; the null distribution runs in the non-frozen harness `crosscheck.py`.
- **Dependencies:** the deps producer reads exactly `hono` + `zod` — the MCP framing is written, not imported.
- **Census:** reconciles in both directions (S121); the reclassified wall is named.
- **Red-team S1–S127:** the statistician (the PBO test can now succeed or fail on the merits — the SE is shown, the distance is a number); the archivist (the chart series is `RETROSPECTIVE`, said); the stranger (the current MCP client is accepted, not refused); the curator (concentration is registrable today; oracle-staleness and utilization are honestly owed, not faked); the attacker (three surfaces baited from a different lab — `guardEfficacy UNJUDGEABLE`, the corpus named the weak wall); the auditor (header and prose agree — S107 + S126); the Operator (two items alone — the question unanswered, the lift's price paid).

**And the last line is the one this project has ended on since V34, computed and unchanged** — the reach and ledger producers read it below. It has never been an engineering problem, and V38 does not pretend to solve it.

---

# PART B — THE SURROGATE ADDENDUM (V38-B, R2): THE PEN MOVED

> **X-DERIVE(a):** the addendum block below is **GENERATED**; the prose **names producers, never restates their values** (S126). This PART B appends to the Substance log and writes **no base-V38 field** (S135).

## THE PEN MOVED — read first. The rulings, verbatim; the inferences, separate; the agent's hands, visible.

For thirteen sprints the gate ended with *presented, never signed*. **This message is the pen.** The Operator ruled on four items in one message, and the agent records each with its inference **stated separately** — because the words are terse, the inferences are load-bearing, and an over-read inference is a forged signature by another name. **Each inference is individually strikeable, and the agent signs nothing.**

| Item | The Operator's words (verbatim) | The inference (separate, strikeable) | Status |
|---|---|---|---|
| **D51** | *"my personal tool"* | ORGΛNON is an **INSTRUMENT** — option (2) of the V36 menu, chosen. n=1 **by design.** Only the V36/V37 pre-priced costs execute. | **ANSWERED** |
| **D33** | *"Break it to understand yourself, red team the math adversly"* | The signature is **WITHHELD**; in its place an **adversarial red team of the frozen core's math**. D33 stays **UNSIGNED** beside its break ledger. | **RE-SCOPED** |
| **D62** | *"try we yourself, and move on (after adverse validation)"* | The circular-gate re-cut is **DELEGATED** — evaluate both, attack both, pick one (**D62-R**), move on. Quoted, bounded, presented strikeable (the one form LN5 permits). | **DELEGATED** |
| **D63** | *"keep it off"* | **STRUCK.** X-CORRELATE unamended; `familyN === 1`; the deflation **INERT**. | **OFF** |

**What the pen did NOT touch, stated as fact (RP-4):** D27 — first in the deviation order for thirteen sprints — remains unsigned, as does the rest of the queue. The pen moved on four items and *past* the queue's head. **The package still ships, D27 still first.** Silence is not a ruling; only words move deviations.

## THE GENERATED ADDENDUM

```
addendum(R2):
  THE PEN:    rulings 4, verbatim · inferences stated separately, each strikeable
              D51 ANSWERED: INSTRUMENT · D33 RE-SCOPED: autopsy ordered, UNSIGNED · D62 DELEGATED · D63 OFF
              D27 STILL FIRST — skipped by the pen, stated as fact (RP-4)
  QUARANTINE: authorship derived (HUMAN|AGENT|FIXTURE) · real := ¬FIXTURE ∧ ¬AGENT · lineage ids moved: 0
  CANARY:     before === after — clean · leaked producer: none (S128)
  D67:        amended criterion drafted, ⟨N⟩ EMPTY + trade-off table (RP-3) · SEARCH appended, real ledger hash in record/ (S137)
              old 8b4e094b PRESERVED beside it · presented: y · pinned by agent: NEVER (LN5)
  D68:        retired: desk study · curator kit · D42 (dissolved: non-commercial) · D50 market clock
              kept: Socket-as-local-interface (INTERPRETATION, flagged, strikeable) · publication: optional-staged
              reach.interpretation: BY-DESIGN, derived (S139)
  MATH RED TEAM (D33): classes run 5 · known-answer PARTIAL (purgedcv + closed-form; papers not reproducibly published, RP-2)
              findings 15 = BREAK 0 · ASSUMPTION-LIMIT 6 (each citing its section) · THEORY-GAP 3 (both readings) · clean 6
              rigor.py bytes moved: 0 · checkFrozenSet: 0 drift (before AND after) · D33: UNSIGNED beside the ledger (S136)
  D62-R:      options attacked: both · pick: A · nothing lit (familyN===1, seeded activation FAILS, S138)
  IN4 DISCHARGED · AF4 STAGED (BLOCKED, no key) · publication optional-staged
  LEFTOVER CENSUS: RETIRED-BY-D68 items each naming their ruling · no orphans
  ADDENDUM KILL-CRITERION (DD-48-R2): canary y ∧ rulings 4/4 y ∧ ledger classified y ∧ IN2 uncontaminated y → SUCCEEDED (RP-5)
```

## THE D51 CASCADE — executed as tasks (only pre-priced costs; the one interpretation flagged)

- **D67 — the market kill-criterion AMENDED, not fired.** It tested the wrong hypothesis (will strangers adopt it?) for an instrument. The `kill-criterion-amendment` producer drafts the amended criterion — *falsifiable by the moat alone* (built on V31's `changedByCompile` + journal, written at decision time, hash-chained, un-retro-fittable) — with **⟨N⟩ left empty and a trade-off table** for the pen (RP-3: a pre-filled N is a pre-filled thesis). The amendment is a **SEARCH** (re-pinning a pre-registered criterion after seeing data is the exact act X-RECKON catches; the tool's own criterion gets no exemption), carrying a real ledger hash in the record hash-chain (the S122 append-site answer, twice). The old criterion (`8b4e094b`) and its text are **preserved beside the amendment forever.** The agent **drafts; the pen pins.**
- **D68 — the retirement ledger.** The desk study, the curator kit, the outreach draft, D50's market clock retire (each naming its reason); **D42 dissolves** — a personal tool is the non-commercial case, and twelve sprints of DeFiLlama exposure evaporate on that fact; the Reach producer's **interpretation derives BY-DESIGN** (the fact never moves, its reading inverts — S139). The **Socket survives as a local interface** — flagged as an *interpretation* of a pre-priced cost, strikeable on its own (channel retires, stdio subprocess stays). **Nothing is deleted; everything is disposed.**
- **D63 — recorded off.** `familyN === 1`; a seeded activation FAILS; the deflated-Sharpe meter stays dark. The builder cost is printed where the builder can see it: *counts and ranks as facts, the meter dark, the memory kept* — and if the pen ever reverses D63, the meter lights over history already recorded, with zero rework.

## THE MATH RED TEAM (D33's order) — *never sheds; the only direct order the pen has ever issued*

The `math-redteam` producer attacked the frozen `rigor` core's DSR/PSR/PBO across **five classes** — known-answer (the closed-form PSR for a symmetric-normal series + the independent `purgedcv` oracle, since the papers' full worked inputs are not reproducibly published, disclosed per RP-2), property (monotonicity, permutation-invariance, scale-invariance), degenerate (constant / single-obs / tiny-T / ties / NaN), adversarial (the i.i.d. trap, extreme skew, odd S), and null-distribution (extending S116). **Every finding is classified into exactly one of BREAK / ASSUMPTION-LIMIT / THEORY-GAP** — and every assumption-limit **cites its assumption by paper section** (RP-1: one that cannot is reclassified BREAK, the burden toward the harsher class). The headline: **no BREAK found under five attack classes — a lower bound, not a proof.** The i.i.d. finding (autocorrelated input overstates PSR/DSR confidence) is an **assumption-limit routed flag-don't-emit**, a render-layer caveat, **never an edit to the frozen formula.** And the standing rule held: the `frozen-set` producer reports **zero drift on `rigor.py` before AND after the autopsy** — an autopsy that edits the body is a forgery, and this one moved not one byte. **D33 sits unsigned beside the ledger, which is where a signature should have to look before it lands.**

## THE QUARANTINE (never sheds) + D62-R + THE GATE

- **The quarantine (S128):** the `Authorship` producer derives HUMAN | AGENT | FIXTURE at the entry path (a sidecar marker outside the hashed surface — **no lineage id moves**); `real := ¬FIXTURE ∧ ¬AGENT` (prove a human, never assume one); a seeded agent lineage **cannot inflate the real count.** The **canary** re-derives clean: the base and quarantined real-class counters agree, no producer leaked. Under D51 this matters more, not less — **IN2's counters are the only validation the project has**, and a poisoned counter in an instrument's self-test destroys the only truth the tool has.
- **D62-R:** both re-cuts attacked against the pinned criteria; the `Recut` producer picks **Option A** (count human-authored SEARCHes, the Proposer gated on the pen) — decoupled from the deflation, so safe under D63-off and if D63 ever reverses; **nothing lights.** Presented at the gate, strikeable.
- **The addendum's success (DD-48-R2):** the `Surrogate` producer computes four legs — the canary clean, the four rulings executed traceably, the break ledger classified, IN2's instrument uncontaminated — and prints **SUCCEEDED** only if every leg holds.

## THE GATE ADDENDUM (presented, NEVER signed — LN5)

```
instrument: BY-DESIGN · the only validation: IN2 · remaining pen acts: D62-R ratify · D67 pin (N) · D27… · D63: OFF (the meter dark, the memory kept) · canary: clean
D51: ANSWERED — INSTRUMENT (the pen: my personal tool) · D33: UNSIGNED beside its break ledger · D62-R: presented (pick A, nothing lit) · D67: presented for the pin (⟨N⟩ open)
LAWS: 17 · minted this addendum: 0 · capability added: 0 · deps: 2 · an instrument for one person keeps all seventeen laws
```

**The first line of the gate is no longer a question — the pen answered it. It is a schedule:** the only validation left is **IN2**; the instrument that will record it is **provably clean** (the quarantine); the fields that will hold the answer were **built in V31, four sprints before anyone knew why.** *A re-litigated ruling, a lit meter, a moved frozen byte, a comfortable classification, a pre-filled N, or a flattering frame is a Halt, not a done.* **The tool is yours, it is honest, and it is waiting to find out whether it changes what you do.**
