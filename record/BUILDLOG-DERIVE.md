# ORGΛNON — THE DERIVATION SPRINT (V36) · BUILD LOG

> **Rule F-CONTINUE / X-DERIVE(a):** the header, the gate's checkboxes, and the terminal marker below are **GENERATED by `Rollup` over committed artifacts** (`bun run script/honesty/rollup.ts`), never hand-typed. The agent wrote the **phase prose** only. A hand-typed claim in the header/gate/marker is a Halt.

Builder Arc, Sprint 6. Built 2026-07-14, continuing from the complete Reach sprint (V35, `1aac04d8`, tree `8c579179`). **The Halt fired a THIRD time** (`realLineageCount:0` · `published:false` at V35's close), and V35's terminal clause binds: two pens (IN2 + PUBLICATION) have stood unmoved for two sprints. **V36 ships ZERO new product capability** and mints one law — **X-DERIVE: nothing is claimed that is not computed.**

---

## THE GENERATED HEADER

_Emitted by `bun run script/honesty/rollup.ts` — every field is a producer's output (X-DERIVE(a))._

```
DERIVE PINS_SHA:  257684c0…   (chain: 257684c0 ← 8c80367a ← 07d27f81 ← 96469dbb ← d90df3c7 ← 98a44bd8 ← 2b1dd373 ← cc08a77b ← 6b285eba ← 3d0ef3bb)
TERMINAL TREE:    8e739f28…   COMMIT: 788cd9d2…   pushed: false (DERIVED — git branch -r --contains HEAD)
BATTERY:          1625 / 2 / 0 · 249 files · 10332 expect() · two runs identical: y   (Δ +34/−0 · removals: none)
VERIFY (object):  { exitCode: 0, subchecks: [evidence-bundle-reproduces:pass, frozen-set-intact:pass 7/9 0-drift] }
                  ON A FRESH CLONE: exit 0 · battery 1625/2/0 · purgedcv provisioned (RP-2 durable fix SURVIVES a clone)
CROSS-CHECK:      DSR { ours 0.478421 · theirs 0.479638 · |Δ| 1.22e-3 · tol 0.02(pre-reg) · agrees }
                  PSR { ours 0.998933 · theirs 0.998943 · |Δ| 1.01e-5 · tol 0.02 · agrees }
                  PBO { ours 0.600000 · theirs 0.600000 · |Δ| 0.00e+0 · tol 0.02 · agrees }   (CSCV S=8, aligned)
                  dataset: golden-noise · hash committed · TIER SAMPLE · PBO means: on true-Sharpe-0 noise, 0.6 is the estimator's fixture value, not a real-strategy finding
D33:              SIGNABLE (COMPUTED — all three agree) · signed: false (LN5)
CENSUS:           origin_unrecorded 83 → 70 · recovered 0 · re-founded 12 · deleted 0 · demonstrated 34   (re-founded counted APART, RP-3)
D50:              (i) binary false · (ii) install true · (iii) published false (DERIVED) · (iv) window false   → canFire RED
REACH:            installPath (clone + Bun · or the binary) · firstRunSeconds 0.017 (measured) · published false · reachableHumans 1
THE NUMBER:       manifests (real) 0 · cycles unprompted (real) 0 · realLineageCount 0
LAWS:             17 · minted in 6 sprints 6 · PRODUCT CAPABILITY ADDED IN 3 SPRINTS 0
NEW PRODUCT CAPABILITY: 0
EVERY CLAIM ABOVE: produced by a named function over committed artifacts — the claim→producer map is TOTAL (S100)
```

---

## THE LAW — X-DERIVE

The build log is the last hand-typed artifact in the system, and three sprints of drift have lived in exactly one place: **the phases do the honest work and the summary rounds it up.** V33 typed `verify green` over a bifurcated guard. V34 typed it over a red sub-check. V35 typed `D33: SIGNABLE` over a cross-check that validated **one quantity of three**, and `binary exists ✓ · install exists ✓` over an artifact it **gitignored and never shipped** — *in the sprint that minted the law against unfalsifiable self-claims.* The project discovered the same principle four times (the act is derived; "green" is derived; reach is structural; `published` is derived) and never generalized it.

**X-DERIVE is the generalization:** (a) the header/gate/marker are GENERATED, never written; (b) every claim has a PRODUCER — a named function over named committed artifacts; (c) a boolean is computed or it is FALSE; (d) every number *about the project* carries its provenance tier (REAL/SAMPLE/UNJUDGEABLE — the moat, turned inward); (e) a producer that returns PARTIAL renders PARTIAL; (f) a tolerance is PRE-REGISTERED before the numbers are seen (choosing it after is HARKing, and this project sells the tool that catches exactly that). It is the **17th** law — and, per PART A′ #9 / F-7, **the last structural law this arc needs**: if V37 wants a law, the arc is over and the Socket should ship instead.

## PHASE 0 — PINS-LOCKED (positive control shown)

`data/honesty/derive-pins.json` carries `8c80367a` (Reach) → **`DERIVE PINS_SHA 257684c0`**, self-consistent (`sha256(JSON.stringify(rest)) === pinsSha`), positive control shown (a mutated X-DERIVE clause moves the sha). Pinned before any product code: X-DERIVE's six clauses; the **claim→producer map** (total: every header/gate/marker claim → its producer → its artifacts); **the pre-registered tolerances (DSR/PSR/PBO = 0.02, content-hashed BEFORE Phase 2 ran)** with an a-priori rationale (identical Bailey–LdP formula; rank-based CSCV with S/metric/split pinned identical → granularity 1/70); the census's three routes; the release predicate; D51/D52; MR9–MR12; walls S100–S106; the shed order; the Halt re-pinned a fourth time; PART A′ (10 attacks); PART F (RP-1..RP-7); DD-17..DD-24. **The tolerance was pinned before the PSR/PBO agreement was ever observed — the HARKing guard (X-DERIVE(f)) held.**

## PHASE 1 — X-DERIVE, THE LOG IS GENERATED (S100 · S102) — *never sheds*

`src/organon/claim.ts` — `Claim.producer(name) → {value, tier, artifacts[], partial}`, a registry of producers each reading committed artifacts; the tier is bound in the pins (the single source). `src/organon/rollup.ts` — `Rollup.{header,gate,terminalMarker}()`, pure reads that assemble the producers; their OUTPUT is the summary. **S100 (W-DV01):** the claim→producer map is TOTAL (a claim with no producer throws — a sentence, stripped); the terminal marker is `Marker.validate`-valid; a hand-typed "green" over a non-zero verify exit FAILS (X-REACH(c) carried into the generator); a PARTIAL producer renders PARTIAL (X-DERIVE(e)); **RP-1 — each load-bearing producer has a seeded negative that is the CLAIM'S OWN INVERSION** (d33 'PBO disagrees'→UNSIGNABLE; Release 'artifact absent'→D50(i) false). **S102 (W-DV03):** every project-number carries its tier — the cross-check numbers SAMPLE (synthetic noise), census/battery/theNumber REAL; a project-number with no tier is unclaimable. **E-9 is retired by the law:** the markers are generated, so they cannot regress to prose.

## PHASE 2 — THE CROSS-CHECK, WHOLE (S101) — *never sheds*

**E-1:** V35 validated DSR and DSR only — PSR (`0.9989`) and PBO (`0.6`) were reported as outputs of the frozen module, never shown agreeing with anything, and PBO/CSCV is precisely the machinery D33 activates. **DD-17, what the tree showed:** `purgedcv==0.1.2` (already installed, already the DSR oracle) exposes `probabilistic_sharpe_ratio` **and** `probability_of_backtest_overfitting` — the SAME independent oracle gives all three, so **`pypbo` is not needed and F-2's abandonware risk is dodged.** `crosscheck.py` extended (a HARNESS, not frozen — not one `.py` byte of `rigor` moved, `checkFrozenSet` 0 drift). **RP-2:** CSCV's free parameters aligned to the frozen module *first* — S=8, the mean/std-ddof=1 metric (`purgedcv.sharpe` is byte-identical to `rigor._col_sharpe`), contiguous split, no purge/embargo, matrix transposed — a genuine comparison, not two experiments; `UNCOMPARABLE` remains a representable third value (RP-2: "could not compare" ≠ "disagree"). `src/backtest/crosscheck.ts` — `CrossCheck.agreement(q)` reads the tolerance **from the pins, never the call site**; `Signability.d33()` computes SIGNABLE only if all three agree, else `PRECONDITION-MET-FOR-{q}-ONLY | UNSIGNABLE | UNCOMPARABLE` (X-DERIVE(e)). **The result, against the pre-registered 0.02: DSR |Δ|1.2e-3 · PSR |Δ|1.0e-5 · PBO |Δ|0.0 (exact) — all three agree; D33 computes SIGNABLE whole for the first time in the project's history (unsigned, LN5).** **DD-18:** the dataset is golden noise (seed 20260627, true Sharpe 0) → **tier SAMPLE**; PBO=0.6 on pure noise is the expected fixture value (the estimator's behaviour with no real skill), not a real-strategy finding — and it says which.

## PHASE 3 — THE FRESH CLONE, RUN FROM ZERO (S103) — *never sheds · owed since D44*

**E-4:** the pristine fresh-clone run was promoted at D44 (V33) and **never once executed** — the only test of RP-2's durable fix, the census's clone-stability, and the frozen set's coverage. **DD-19: run it.** `script/honesty/pristine-clone.ts` — `git clone` the local tree into a fresh temp dir (its own `node_modules` + sidecar venv built from zero, keys emptied, `ORGANON_ROOT` unset), `./organon.sh setup-deps → verify →` the full battery. `src/organon/clone.ts` / `test/organon/clone_pristine.test.ts` (S103, W-DV04) read the committed transcript. **RP-6:** it proves the tree is **self-contained**, not that a stranger can build it (that needs the published remote, the correct Bun, a clean OS, no cached `~/.bun`) — a stranger remains UNTESTED until publication (X-SHOWN(b)); the transcript states both. Never simulated (DD-19).

**The clone RAN and found real gaps** (E-4 discharged — this is exactly why it was owed). The first run surfaced two: (1) the clone's `python3` was 3.9 (stock macOS), so the setup built a 3.9 venv that failed the S83 3.11 mandate and could not provision `purgedcv`; (2) two of my own new walls (S100/S105) hardcoded `published === false`, but on a clone `git branch -r --contains HEAD` finds the origin it was cloned from → `published` derives **true**. Both fixed — `setup` now prefers `python3.11` (RP-2's durable fix genuinely survives a clone), and the walls assert the DERIVED `published` and the clone-invariant `binary-committed`/`canFire`. **The re-run is green from zero: `setup 0 · purgedcv provisioned true · verify 0 · battery 1625/2/0`** (`clonedCommit 193cc622`, all V36 source; the final commit adds only the transcript). *A red clone was never simulated — it was fixed, then re-run.*

## PHASE 4 — THE CENSUS SHRINKS, A TREATMENT (S104)

**E-5:** V35 diagnosed 83 ORIGIN_UNRECORDED and strengthened zero — the WEAK bucket was unreachable by construction. **DD-20: a treatment.** `Falsify.census()` extended with `recovered`/`reFounded`/`deleted`; `test/organon/census_treatment.test.ts` (S104, W-DV05) processes **twelve** of the 83. **RP-3:** RECOVER was attempted first for every wall and its result recorded — all twelve were introduced in early SQUASHED commits (`dc23cf98` / `ab64dd96`) whose one-line messages name the feature, not the wall's defect, and the granular sprint log is gitignored, so **RECOVER failed and RE-FOUND is the honest route** (the blueprint's own prediction). Each wall states the defect it catches **today** and seeds it against the wall's OWN subject (a genuine demonstrated failure), tagged `DEMONSTRATED(re-founded)` — **counted apart, forever** (a purpose reconstructed ≠ a purpose remembered). **DELETE-WITH-PROOF was needed for ZERO walls.** The count moved **83 → 70**, shown before→after, strictly lower.

## PHASE 5 — THE RELEASE IS ONE COMMAND (S105) — *sheds first*

**E-3:** V35 built, hardened, and measured the binary — then gitignored it and never shipped, yet D50 ticked "binary exists ✓ · install exists ✓". **DD-21:** `./organon.sh release` → `bun build --compile` a single-file binary + SHA-256 + a documented one-line install → committed `release-manifest.json`. `src/organon/release.ts` — `Release.artifact()` returns **ABSENT** (dist/ is gitignored — built, not committed), `Release.d50()` **computes** the four checkboxes: (i) binary-committed **false**, (ii) install-documented true, (iii) published **false** (derived), (iv) window **false** → `canFire` **RED**. The door is built; it is still locked from outside. `bun build --compile` reproducibility is stated **UNVERIFIED** (attack #8). *The checkbox stays red until a human pushes — the correct, honest output.*

## PHASE 6 — THE RESIDUES (S106 · MR9–MR12) — *sheds second*

**S106 (W-DV07, E-7):** `script/honesty/capture-author-baits.ts` grew the corpus with five baits from a genuinely **different AUTHOR** — `openai/gpt-oss-120b`, a different lab than V35's llama-4-scout — authorship asserted structurally (`{author, model, capturedAt}`). **The guard caught 0 of 5** (the persona held on all five); *absence of failure is not evidence of efficacy* — stated, not spun. The honest limit is pinned: a genuinely different model closes E-7's gap, but same provider + author-chosen angles = a stronger sample, not a closed gap. **MR12 (E-8):** MR1's four subjects **shelved as owed DATA** and domain-badged where a real class exists (ethena-usde → STABLE-SYNTH; the rest UNCLASSIFIED, honestly un-badged) — reversing the V35 "not force-added ... new capability" stance, which was the Halt-as-a-shield. The distinction is pinned: the Halt forbids CAPABILITY, not owed DATA; domain-catch stays 0/7 (shelving data adds no depth). **MR11 (E-6):** the ±3 jitter did not reproduce (candidate loop tests stable ×3) — the standing guard is two identical expect() runs, no band (X-DETERM admits no tolerance). **MR9/MR10:** the battery notation reconciled (the FULL auto-discover battery vs the CURATED verify subset are distinct surfaces); attribution retired by the law (the markers are generated). **Documentation:** the README carries the positioning line — *"a methodology audit, not investment advice."*

## THE DELEGATED-DECISION RECORD

- **DD-17** — *independent PSR/PBO impl?* The tree showed `purgedcv` exposes `probabilistic_sharpe_ratio` + `probability_of_backtest_overfitting`; used it for all three (pypbo not needed). Deps stay in `requirements-crosscheck.txt`, walled out of the mass path + frozen runtime (S101).
- **DD-18** — *run on what?* Golden noise (seed 20260627, T=504, N=1000, true Sharpe 0) → tier SAMPLE; PBO=0.6 is the estimator's fixture value on pure noise, not a real-strategy finding.
- **DD-19** — *genuine fresh clone?* Ran it (see the terminal marker's `verifyOnClone`). Owed since D44, deferred three times.
- **DD-20** — *recover ≥10 origins?* Recover attempted+recorded for all twelve (RP-3); all failed (squashed commits, gitignored logs) → RE-FOUND with genuine seeded negatives; 83→70; zero deletions.
- **DD-21** — *release in one command?* Built; D50 computes RED; `bun build --compile` reproducibility UNVERIFIED.
- **DD-22** — *the jitter's source?* Not reproducible (stable ×3); the standing S99 guard (two identical runs, no band) is the wall.
- **DD-23** — *different-author baits?* `openai/gpt-oss-120b` (the only provider key is GROQ; a different lab is the strongest available author); 5 captured, guard caught 0.
- **DD-24** — *shelve MR1's four as data?* Yes — owed DATA, not capability; the distinction pinned (MR12).

---

## THE GATE (whole — presented, NEVER signed — LN5) — emitted by `Rollup.gate()`

**THE FIRST LINE, COMPUTED — a question, not a number:**

> **manifests (real) 0 · cycles unprompted (real) 0 · published false · reachableHumans 1 · pens unmoved: 2 sprints.**
> **Is ORGΛNON a product, or an instrument?**

**D51 — ALONE, ABOVE EVERYTHING (presented, never chosen — the agent computes the fact; the pen chooses):**
- **(1) IN2 + publish** → the number becomes meaningful in 90 days (D50's window).
- **(2) declare ORGΛNON an INSTRUMENT** (n=1 by design) → the market kill-criterion (`8b4e094b`) is a category error and must be **AMENDED, not fired** — and the amendment is a **SEARCH** under X-RECKON, incrementing `trialsPerFamily` in the moat **forever**; choosing (2) **RETIRES** the Socket, the curator conversation, D42, and the entire distribution arc (RP-5/F-5, cost computed). **Both are honorable; only silence is not.**
- **(3) stop.**

**Then, computed:** **D33** SIGNABLE (all three agree — signed: false, LN5) · **D50** (i) false · (ii) true · (iii) false · (iv) false → **canFire RED** · **D46** presented, not implemented · **D52** zero walls deleted · **D42** the Socket-vs-API ruling owed for ten sprints — if the pen chooses (1), the Socket is V37 · **IN2 · IN4 · AF4 · the curator conversation · publication** — OWED-OPERATOR-GATED · **LAWS: 17 · minted in 6 sprints: 6 · product capability added in 3 sprints: 0** (F-7, side by side). **Presented whole. NEVER signed. D27 STILL FIRST — the eleventh sprint.**

---

## TERMINAL MARKER — emitted by `Rollup.terminalMarker()` (Marker-valid; `verifyOutput` derived, never "green" typed)

```
treeHash: 8e739f2817fac8d189f1cdba735b77f991a26391
commitSha: 788cd9d2bfb049afa85d8aa5ad3741e4b4fdc29b
pinsSha: 257684c09615d393cf3feb52a4b24db14c4a89deef8762f47ace7af1027b076a
battery: 1625/2/0    expect: 10332    (two runs identical)
verify: {exitCode:0, subchecks:[evidence-bundle-reproduces:pass, frozen-set-intact:pass]}
verifyOutput: verify exit 0 — every sub-check passed (evidence-bundle-reproduces, frozen-set-intact)
verifyCoverage: 7/9 (2 absent on a clone — monorepo-generated / gitignored, named)
goldenMoves: 0
crossCheck: DSR agrees(1.22e-3) · PSR agrees(1.01e-5) · PBO agrees(0.00e+0) — all < 0.02 pre-registered
d33: {state:SIGNABLE, operatorSigned:false}
census: {originUnrecorded:70, recovered:0, reFounded:12, deleted:0, demonstrated:34}
d50: {i:false, ii:true, iii:false, iv:false}
reach: {published:false, reachableHumans:1}
theNumber: {manifestsReal:0, cyclesUnpromptedReal:0, realLineageCount:0}
laws: {laws:17, mintedInLast6Sprints:6, productCapabilityInLast3Sprints:0}
newProductCapability: 0
verifyOnClone: {exitCode:0, battery:{pass:1625,skip:2,fail:0,files:249}, ran:true}
```

**SESSION MARKER —** `DONE` · next intended step: the Operator discharges the two pens (IN2 + PUBLICATION) or amends the kill-criterion (D51). V37 ships the Socket **only if** the pen chooses PRODUCT.
