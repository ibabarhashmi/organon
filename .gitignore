node_modules/
src/backtest/py/.venv/
**/__pycache__/
RWA-VERDICT.md
data/snapshot/
data/feeyield/
data/lending/
*.log
.DS_Store

# secrets — API keys are server-side env-only, NEVER committed (X-BYOK key-safety). .env is local + gitignored.
.env
.env.*
!.env.example

# sprint planning blueprints — not part of the published product
sprint/sprint-result/ORGANON_*Blueprint.md
sprint/sprint-result/ORGANON-*Blueprint.md
sprint/sprint-result/ORGΛNON_*SPRINT.md

# data-plane captured snapshots — gitignored (A′#12); the nonce-anchored provenance.jsonl chain is the committed guarantee
data/dataplane/snapshots/

# impeccable (the design dev-seam — Surface sprint, X-SURFACE b) — DEV-TIME-ONLY: the tool + its working/ephemera files
# NEVER ship on the mass path; the whole dir is gitignored EXCEPT config.json (the committed reasoned detector-exception
# record, kept as dev provenance). A runtime import of impeccable / any CSS framework is a Halt — deps stay hono+zod.
.impeccable/*
!.impeccable/config.json

# sprint logs — internal build telemetry, not part of the published product
sprint-log/

# telemetry + feedback — local-first capture, opt-in, NEVER committed (X-TELEMETRY)
data/telemetry/
