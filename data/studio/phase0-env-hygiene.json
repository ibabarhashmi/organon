{
  "protocol": "phase0-env-hygiene",
  "rule": "F-ENV (clean the suspects whether or not they are convicted), A'#9, T1-01",
  "at": "2026-07-04",
  "sprint": "Warranty (v8) Phase 0",
  "candidateEnvironments": {
    "note": "The three Python venvs found across the machine — the candidate matrix Phase 1 will regenerate the RWA verdict under. numpy differs: this is the prime environmental suspect for the NOT-YET -> DISQUALIFIED drift.",
    "standalone": { "path": "/Users/babar/Projects/organon-standalone/src/backtest/py/.venv", "python": "3.9.6", "numpy": "2.0.2", "scipy": "1.13.1", "pandas": "2.3.3", "created": "2026-07-04 (fresh, via requirements-studio.txt)" },
    "monorepo-old-tree": { "path": "/Users/babar/Projects/organon/packages/solidity-sentinel/src/backtest/py/.venv", "python": "3.9.6", "numpy": "1.26.4", "scipy": "1.13.1", "pandas": "2.3.3", "note": "the pinned-era environment; holds the RWA generator + engine + market data" },
    "stale-checkout-SENTINEL": { "path": "/Users/babar/Projects/SENTINEL/organon/packages/solidity-sentinel/src/backtest/py/.venv", "python": "3.9.6", "numpy": "1.26.4", "scipy": "1.13.1", "pandas": "2.3.3", "note": "the relocated/copied-venv suspect; RETIRED this phase" }
  },
  "shebangHazard": {
    "finding": "the venv's pip/console-script shebangs hardcode an ABSOLUTE interpreter path (e.g. #!/Users/babar/Projects/organon-standalone/src/backtest/py/.venv/bin/python3) — a copied/relocated venv breaks",
    "shipped": false,
    "repair": ".venv is gitignored (src/backtest/py/.venv/) and NOT tracked (0 files in git) — a fresh clone REBUILDS the venv via `python3 -m venv`, regenerating correct shebangs. The hazard only bites a COPIED venv (what the transplant tooling did); the documented setup never copies, it rebuilds. Documented, not shippable.",
    "status": "REPAIRED-BY-CONSTRUCTION (rebuild-not-copy) + DOCUMENTED"
  },
  "staleCheckoutRetired": {
    "path": "/Users/babar/Projects/SENTINEL/organon",
    "remote": "github.com/ibabarhashmi/organon.git",
    "head": "6e04d4a (add product logo to README)",
    "action": "wrote _RETIRED.md pointer note at its root; NOTHING deleted (T-POLLUTION)",
    "reason": "stale parallel checkout, environment-drift suspect; retired so no build/regeneration runs against it"
  },
  "requirementsStudio": {
    "file": "src/backtest/py/requirements-studio.txt",
    "shipped": true,
    "pointedAtBy": "organon-setup.sh + .github/workflows/studio-battery.yml + SKILL.md",
    "currentlyPinned": false,
    "note": "requirements-studio.txt currently lists bare `numpy`/`scipy` (unpinned). A fresh py3.9 install therefore resolves numpy==2.0.2 (the standalone's env) while the pinned-era monorepo has 1.26.4 — the exact drift vector. DELIBERATELY not pinned in Phase 0: pinning to a version before the forensics says which numpy reproduces the pinned NOT-YET verdict would PREJUDGE the classification. Phase 1 (F-ENV) produces the hashed lockfile pinning the reproducible version, derived from the candidate matrix — not decreed here."
  },
  "rwaVerdictArtifact": {
    "file": "RWA-VERDICT.md",
    "gitignored": true,
    "committed": false,
    "note": "RWA-VERDICT.md is a GENERATED artifact (gitignored) — the standalone carries only the frozen.ts PIN of it, and cannot itself regenerate it (the generator + engine live in the monorepo — inventory absence `rwa-verdict-regeneration`). This is precisely the F-ENV question: a pin over an output this tree cannot reproduce."
  }
}
