{
  "protocol": "legacy-disposition",
  "inScopeGreenFloor": {
    "suites": 24,
    "tests": 119,
    "expects": 256,
    "note": "walls + studio surfaces/routes/screens/graph/agents/capture/clocks/enroll + ledger/laundering/refragmentation/durable — ALL GREEN"
  },
  "clockAdjacentAlibiExpired": "studio_capture + studio_clocks run GREEN against real captured stamps (data/studio/clock-stamps.jsonl is committed); the clock capability has NO missing-data alibi and needs none.",
  "dataAbsenceWaivers": [
    {
      "suites": [
        "test/redteam/frozen_integrity.test.ts"
      ],
      "absentArtifacts": [
        "RWA-VERDICT.md",
        "data/feeyield/forward/<date>/MANIFEST.json"
      ],
      "rule": "S-CORE data-plane (gitignored)",
      "disposition": "WAIVED — pinned criteria do not require operator-local frozen artifacts on a fresh clone; core byte-identity is proven fresh-clone-green by test/walls/core_byte_identity.test.ts"
    },
    {
      "suites": [
        "test/redteam/wall_frozen.test.ts"
      ],
      "absentArtifacts": [
        "RWA-VERDICT.md"
      ],
      "rule": "VII",
      "disposition": "WAIVED — same class; the sha-check gate cannot fire without the generated verdict artifact"
    },
    {
      "suites": [
        "test/redteam/frozen_integrity.test.ts (W4)"
      ],
      "absentArtifacts": [
        "data/snapshot"
      ],
      "rule": "VII",
      "disposition": "WAIVED — DataAdapter swap test needs the snapshot; run scripts/snapshot.ts to enable locally"
    },
    {
      "suites": [
        "test/organon/stress_downside.test.ts (W2/W2.5)"
      ],
      "absentArtifacts": [
        "data/snapshot"
      ],
      "rule": "III/XXI",
      "disposition": "WAIVED — downside-edge + leak-tripwire need the snapshot"
    },
    {
      "suites": [
        "test/organon/attest_hardening.test.ts"
      ],
      "absentArtifacts": [
        "data/snapshot"
      ],
      "rule": "XIV",
      "disposition": "WAIVED — the 'caller returns never earn V2' test re-executes the engine's own snapshot data"
    },
    {
      "suites": [
        "test/organon (Phase 13 read-only tools)"
      ],
      "absentArtifacts": [
        "data/snapshot"
      ],
      "rule": "Phase 13",
      "disposition": "WAIVED — marketdata/risk/backtest tools need the snapshot"
    },
    {
      "suites": [
        "test/organon (Phase 14 verdict + Appendix O)"
      ],
      "absentArtifacts": [
        "data/snapshot"
      ],
      "rule": "Phase 14",
      "disposition": "WAIVED — verdict battery needs the snapshot"
    },
    {
      "suites": [
        "test/organon (Lending-Carry HARDENING Phase 2)"
      ],
      "absentArtifacts": [
        "data/lending"
      ],
      "rule": "Appendix C",
      "disposition": "WAIVED — utilization/omitted-variable need the lending snapshot"
    },
    {
      "suites": [
        "test/feeyield/*.test.ts"
      ],
      "absentArtifacts": [
        "data/feeyield",
        "data/funding"
      ],
      "rule": "fee-yield domain",
      "disposition": "WAIVED — exposure/gradient/residuals need the fee-yield + funding data plane"
    }
  ],
  "waiverBasis": "these are the disclosed gitignored-data-absence class, documented since V3/V4; every waiver names the absent artifact and the script that regenerates it. NONE are studio/walls/ledger/clock suites. The three-sprint wave-through ends: each line is dispositioned green-or-named-absent, not blanket-skipped."
}
