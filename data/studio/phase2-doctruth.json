{
  "protocol": "doc-truth",
  "driftFound": [
    "no doc gave a concrete 'serve' command for the studio",
    "no doc gave a concrete submit_spec curl with a real body",
    "no doc gave a concrete example spec (SKILL.md only pointed at src/strategy/spec.ts)"
  ],
  "fix": "added a copy-paste 'Quickstart (literal)' to skill/SKILL.md: serve → curl submit_spec (example spec) → read attestation",
  "literalPathVerified": {
    "steps": "bun run script/serve-studio.ts → curl POST /studio/submit_spec (the doc's exact example body)",
    "result": "REGISTERED (ledgerSeq:1) → verdict INSUFFICIENT-EVIDENCE, tier V0, family 1 — EXACTLY as the doc predicts (5 return points < the ~225 floor)",
    "conclusion": "a clean session now follows the docs literally from nothing to a registered verdict (DOC-DRIFT closed)"
  }
}
