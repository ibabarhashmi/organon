{
  "protocol": "reachability",
  "asOf": "2026-07-04",
  "servedUrl": "http://localhost:4319",
  "reachedFrom": "a SEPARATE curl process (not the serving Bun process/author-session) — reachable, NOT independent (L-2P)",
  "evidence": {
    "GET /health": "{\"ok\":true,\"trials\":1} — 200",
    "GET /trust": "Trust Panel raw state (walls 12/12, clocks scheduler-originated ×5 TICKING, battery 135/0/27, inventory anchor bee1a152, independence PENDING)",
    "GET /": "dashboard renders the Trust Panel section, scheduler-originated TICKING, independence PENDING, EMPTY OF GO",
    "POST /studio/submit_spec (no token)": "401 unauthorized (authn fires over the wire)",
    "POST /studio/submit_spec (Bearer token)": "200 (the credential opens the mutating door; verdict path unchanged)"
  },
  "tenseNote": "REACHABLE is earned (reached by a party other than the serving author-session, C-TENSE). INDEPENDENCE is NOT (the reaching curl is still author-operated). A genuine non-author second party + a public tunnel/deploy remain OPERATOR-gated (L-2P); script/tunnel.sh makes a public URL a one-command operator step."
}
