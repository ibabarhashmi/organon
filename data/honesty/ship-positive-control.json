{
  "protocol": "ship-positive-control",
  "at": "2026-07-15",
  "rule": "RP-1 (F-1 CRITICAL) — the positive control on the REAL emit path: a seeded bad marker slot, the REAL emit command, and NO build log written (a refusal at the same path instead). No unit test satisfies this control (X-SHOWN(b)).",
  "command": "bun run script/honesty/ship.ts --seed-bad treeHash --out sprint/sprint-result/ship-pc-output.md",
  "seededSlot": "treeHash",
  "wrote": "refusal",
  "refusal": {
    "wall": "S152",
    "artifact": "the terminal marker",
    "value": "Marker.validate FAILED — missing [] · invalid [treeHash: not a 40-hex git tree object (a hand-typed value, not a re-derived hash)]"
  },
  "outIsRefusalNotLog": true,
  "realLogUntouched": "the real build log did not exist before this control run"
}
